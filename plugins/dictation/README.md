# Dictation

Bundled UI-only extension for the home composer's dictation button. It executes no backend code and requests no data or action authority. Disable or unlink `muxr.dictation` to hide the button; Live Voice (`muxr.voice`) is unaffected.
