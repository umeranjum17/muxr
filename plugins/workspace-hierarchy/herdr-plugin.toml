id = "muxr.workspace-hierarchy"
name = "Workspace Hierarchy"
version = "0.1.0"
min_herdr_version = "0.8.0"
platforms = ["linux", "macos"]
description = "Source-driven workspace tree built from bounded public host context"
