# Probe

Walking-skeleton client. Presentation only: opens a loopback WebSocket and asserts contract events plus a request round-trip against a `--fake` host.

No application layer. It is an adapter that exercises StartAgent / ListAgents over the wire.
