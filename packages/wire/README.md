# @muxr/wire

Private workspace package containing muxr's shared wire types and schemas.
It is built with the repository and is not published independently.

```bash
yarn workspace @muxr/wire build
yarn workspace @muxr/wire test
```

Product licensing and inherited third-party attribution are maintained at the
repository root in `LICENSE`, `NOTICE`, and `LICENSES/`.
