---
title: Release and polish task tracker
slug: release-polish-todo
status: in-progress
created: 2026-09-05
updated: 2026-09-06
owner: astra
links:
  - https://github.com/umeranjum17/muxr/pull/224
  - ./release-channels.md
---

# Everything we discussed: release and polish TODO

Snapshot: 6 September 2026. This is the session-wide checklist, not a claim that every item is in the current APK. **Checked means the stated scope is done. Implemented, merged, tested and available on your phone are different milestones.**

Current product PR: [#224](https://github.com/umeranjum17/muxr/pull/224), draft. Release #209 and follow-ups through #223 are merged. Production promotion remains deferred until you request it. No automated beta has published successfully yet.

## Beta release authorized: publication in progress

The beta release is authorized and being prepared. Concept B, the anchored list, is the layout you selected after Fable's three concepts were checked against Metro screenshots; GLM 5.3 Flash developers built it through visible OpenCode panes, an independent Opus front-end review drove the corrective passes, and the lead's visual-fidelity pass closed it against the selected concept on native normal, 320dp at 1.3 font scale with a real keyboard, native dark, and web 320 and desktop 1280×900 in light and dark. UI iteration now runs on the existing Metro dev client with one release pass after changes settle, because the repeated release-validation loop was the bottleneck.

Release build V5 is prior, pre-patch acceptance evidence and no longer the candidate: on that older source it PASSes the complete 34/34 suite, the full native gate on rerun after a harness-only fixture fix, the rich preview and attachment polish flows, and 40 of 40 release cold starts. It does not carry the runtime-reload fix. The current candidate is release build V6 from source sha bda55f208173ef540efac149fcdf51f39802ad469385097e861486ee98a81633, and it PASSes: the release build, its focused polish flow on a byte-identical installed APK, and an actual release Settings logout that exercises the real teardown path. The observed AnimationFrameCallback teardown crash is fixed and verified within that scope; [Current results and evidence](#current-results-and-evidence) states the exact limits of that claim. What remains is your acceptance on a physical phone. Nothing here says an item is on your phone.

Digests, artifact names, per-run outcomes and the carried limitations live in [Current results and evidence](#current-results-and-evidence). The user authorized the beta merge and publication on 6 September 2026; release execution is in progress. The local installed host is a separate, already-taken decision: you explicitly authorized the update, 0.1.26-dev.local.20260906.62b4e99d is installed, and it delivered the next realtime message successfully.

## Current results and evidence

- **Current V6 identity.** Source `bda55f208173ef540efac149fcdf51f39802ad469385097e861486ee98a81633`; mobile `4da4aa6667bbdf1ecc516ef61bd6e68fe0b96e8b51d3516b11b322fe8353849d`; release APK `3059c3d5806fd92e78b320e6a6249c402a3427fea1d6c7fe89618753636d76d8`. The 40 animated reloads use the debug APK from this source; focused polish and logout use the V6 release APK. Both are x86_64 emulator builds, not physical-phone acceptance.

- **Prior V5 source and artifact identity (historical).** Workspace source digest 62b4e99d134f2824c2f46af9c594e1a4e8746dcc30f658eb9021fbd9aaa77874, mobile digest 3ed846cc43f9880226bdee8ed5d0cbedd0797db027e2e819c4198e69bbc2bb39, APK SHA-256 b27870b6c6f98316a63fc2483881d7fa68679d8c04b643b1da6e6e8112ef1d81, provenance in pane-actions-final-release-v5-provenance.json. The APK is an x86_64 test-signer build, not the phone artifact.
- **Release build and suite.** Release build V5 PASSes at 09:52:29Z; the complete local suite PASSes 34/34 in 172.1 seconds, finished 09:55:50Z (pane-actions-final-release-v5-suite.log and -result.json in the lead watcher roots).
- **Full gate, first run (superseded).** Completed FAIL on the graphics checkerboard only; every other phase and feature passed, including the standalone Changes coverage with report.changes and branch/working/staged screenshots, text IME and drag, and usage (full/report.json and graphics-pixels.png retained). Grounded in perf/prGate.mjs, the full document phase calls viewerControls(), changesScopeControls() and viewerLineTarget(), so a passing full gate already runs the standalone Changes flow and no duplicate run is needed.
- **Harness fix, harness only.** The failure was fixture selection: the checkerboard producer is pinned to the first pane while a fixed coordinate tap under live card churn selected another working pane, whose screenshot showed a positively resizing terminal. The text phase now opens the pinned proof pane through its real persisted session binding and asserts both the mounted pane header and a fresh positive-cell-dimension terminal.resize for that exact pane id before any pixel is sampled; observer thumbnail reads carry no cell dimensions and no longer count as attach proof, and the checkerboard thresholds are unchanged. No product, mobile or APK bytes change.
- **Full gate rerun (r1) — PASS.** Completed 10:33:53Z with no failures, on the same APK and source bytes (pane-actions-final-release-v5-r1-full/report.json in both lead watcher roots). All four performance phases pass (herd, document, terminal-text, graphics), alongside the persistent viewer with file, hunk, line-200 targeting and offline recovery, Changes branch/working/staged, text and graphics native keyboard with the closed-puck drag, and usage. The proof pane resolved to w1:p1 with header · 1/4 and two fresh native resizes at 19×37 cell pixels, and the checkerboard sampled 24977 magenta and 25111 teal of 53592 samples, holding at 24950/25104 after the native keyboard resize.
- **Verified UI scopes.** Post-fidelity web panel at exactly 268px with real Close/Files/Tools navigation and zero errors after the three-origin allowlist fix; native compact and keyboard smoke with a real IME at 320dp and 1.3 font scale, every row reachable and Close pinned; native dark and web desktop 1280×900 light and dark with appearance restored to Adaptive; the Files/Tools repeat passing a 1.2s transition settle without source changes. Colored Changes totals pass a real 52-file Git scenario, and source-plugin-runtime-smoke.json proves the managed checkout-plugin path: nine bundled plugins load through checkout paths with registration, enabled states and unrelated plugins preserved, automatic terminal sibling linkage, private permissions and a clean runtime removal.
- **Rich previews (rich flow) — PASS.** Finished 10:36:42Z with no failures on the same V5 APK and source and the frozen harness (pane-actions-final-release-v5-rich/report.json in both lead watcher roots): Markdown and HTML render real native content proven by the native hierarchy, CSV by screenshot OCR, and the PDF shows two colored pages with working page navigation. This flow measures features, not performance; the four performance phases come from the full gate.
- **Attachment polish (polish flow) — PASS.** Finished 10:39:23Z with no failures on the same bytes and harness (pane-actions-final-release-v5-polish/report.json in both lead watcher roots): installed version 0.1.26 reaches settings, composer upload, thumbnail, preview, real pixel zoom and pan against their movement thresholds, and removal all pass. Feature scope only; performance stays with the full gate.
- **Release cold starts — PASS.** Finished 10:45:49Z with zero failures on the exact V5 APK: 40 of 40 restored cold starts reach the foreground with no new crashes (pane-actions-final-release-v5-cold-starts.json and .log in both lead watcher roots). Scope is the normal release APK, not an ASan build and not physical-phone acceptance.
- **Root-reload JNI abort — fixed and verified within scope.** This supersedes the v3 attribution, which stays in the history appendix as history. A merged upstream fix does match the observed AnimationFrameCallback abort: PR 10278, commit b3157cd97388554174449cbb5466944749618747, "stop AnimationFrameQueue when WorkletsModule is invalidated". Upstream implements it in Kotlin; it is backported here as Java against the installed Worklets 0.7.2, together with a Reanimated pre-invalidate barrier that upstream does not have: NativeProxy.invalidate() stops the shared frame queue before invalidateCpp() releases javaPart_, because TurboModuleManager invalidates Reanimated's module holder before the Worklets one, so Worklets cannot be relied on to stop the queue first. Scope is the observed AnimationFrameCallback path. Evidence, with its limits stated: the controlled real-queue Java flow is the only deterministic before-and-after, failing without the change and passing with it. The patched native stress then PASSes 40 of 40 at 2026-09-06T11:32:09.686Z with zero failures, crashes or hangs, eight actually advancing Reanimated loops per reload, one process (pid 20673) and a fresh JS runtime on each reload (runtime-reload-patched.json, with runtime-reload-patched-build-provenance.json for the APK; source sha bda55f208173ef540efac149fcdf51f39802ad469385097e861486ee98a81633). That stress bounds behaviour rather than demonstrating a native before-and-after: the unpatched baseline also passes its 20 reloads, so the native runs do not reproduce the abort on demand and must not be read as a native crash reproduction. Release build V6 PASSes at 11:35:11.735Z and its focused polish flow PASSes at 11:38:34.320Z with no failures on a byte-identical installed APK (sha256 3059c3d5806fd92e78b320e6a6249c402a3427fea1d6c7fe89618753636d76d8; runtime-reload-release-v6-polish/report.json). An actual release Settings logout PASSes at 11:39:09.836103Z: the process survives, the pairing screen returns, and the logs prove the real teardown ran — getOrCreateReloadTask, ReactContext and ReactInstance destroyed, surfaces restarting — with zero new fatal signals and only the retained non-fatal soft-teardown warnings (runtime-reload-release-v6-logout.json and .log with screenshots in both lead watcher roots). That is one actual reload, not reload stress. Within that stated scope the crash is fixed and verified; it is not carried as an accepted beta limitation. Outside it, nothing is validated on a physical phone or on ARM64, and nothing is published.
- **Runtime-reload path.** dev-fidelity-crash-runtime-boundary.md records that logout/reset reaches an app-accessible runtime reload through Updates.reloadAsync even with expo.modules.updates.ENABLED=false, since the installed expo-updates DisabledUpdatesController restarts the React application; ordinary activity lifecycle destruction is a separate path. That path is now observed rather than inferred: a Settings logout on the owned V5 release build reached the pairing screen in 14 seconds under the same process id with no new fatal signals, and its logs show the real reload task, the RecreateReactContextProcedure restart, the ReactContext and ReactInstance being destroyed and surfaces restarting (release-logout-smoke.json and .log with before and after screenshots). The abort did not reproduce there, but one logout is not race stress and does not prove it absent.
- **Still open.** Your acceptance on a physical phone, which no emulator run substitutes for, and the exact ARM64 phone beta and npm artifacts, which only exist after an authorized merge and release workflow. The remaining beta tasks stay deferred until release at your direction.

## Next execution order

Ownership at the freeze: the lead coordinates and owns all acceptance and every emulator, browser and build run. An Opus verification agent took over the performance harness and this documentation after GLM 5.3 Flash reached its weekly cap mid-edit; a separate Opus front-end agent owns the UI and is frozen. The earlier GPT-6 Astra delegation instructions are history, not current authority.

Completed locally on the frozen final source, in order: the dev APK native hideKeyboard build, release build V5, the complete 34/34 suite, the full native gate (first run failed on a harness fixture; rerun r1 PASSes on the exact APK after a harness-only fix), the rich preview flow, the attachment polish flow and 40 of 40 release cold starts; the four-job acceptance sequence reports code 0 for every job. Per-run detail and digests are in [Current results and evidence](#current-results-and-evidence).

1. Your acceptance on a physical phone; no emulator run substitutes for it, and nothing here claims an item is already on your phone. The runtime-reload crash is closed within its stated scope, and the remaining beta tasks stay deferred until release.
2. Leave the checkout ready for a merge discussion. No merge or beta publication happens without you.
3. Promote production only after your acceptance.

## Release and delivery

- [x] **T01 · Consolidate the release and close superseded feature PRs** — Done, within stated scope.
  - Evidence / current state: Release #209 and follow-ups #211–223 are merged. Current product work is together in draft #224; unrelated dependency PRs remain open.
  - Remaining / boundary: Continue adding this polish to #224; review dependency upgrades separately.

- [x] **T02 · Implement dev / beta / manual stable channels for npm and Android** — Done, within stated scope.
  - Evidence / current state: Merged #222; exact green source, immutable artifacts, channel-aware updates, Android build-number ledger, separate dev app identity. Local package/native identity checks passed.
  - Remaining / boundary: Production stays manual; beta uses the existing app identity and data.

- [x] **T03 · Fix the first beta pipeline failure** — Done, within stated scope.
  - Evidence / current state: Merged #223: ignore generated Bundler cache paths without weakening source provenance. Normal CI passed.
  - Remaining / boundary: This fixes the diagnosed build blocker; it does not prove publication.

- [ ] **T04 · Publish the first successful automated beta** — Not done.
  - Evidence / current state: Authorized beta release remains unpublished. Merge and release are held for the user-reported severe black flashing during actual Code/Browser interactions.
  - Remaining / boundary: Fix and verify frame continuity, consolidate evidence, then merge exact green source and publish signed ARM64/npm beta. Stable remains deferred.

- [x] **T05 · Provide an installable phone preview** — Done, within stated scope.
  - Evidence / current state: Signed ARM64 preview build 356 is public at preview-840708b0, linked in #222. Application/native parity and prior local gate passed.
  - Remaining / boundary: It predates #224. Do not mistake it for the new settings, zoom or thumbnails build.

- [ ] **T06 · Attach the latest polished APK to the PR and notify you** — Not done.
  - Evidence / current state: 7ce46ade native build, full/rich/polish emulator gates and CI passed. Follow-up Changes and actual graphics magnification are committed and awaiting their fresh combined device run.
  - Remaining / boundary: No latest ARM64 beta has been published yet; deliver its verified immutable artifact after acceptance.

- [x] **T07 · Explain every release feature with first principles and diagrams** — Done, within stated scope.
  - Evidence / current state: Consolidated PR224 includes first-principles behavior, Mermaid diagrams and evidence boundaries per feature. The Changes scope and real graphics magnification entries are included in the current update.
  - Remaining / boundary: Final delivery table must follow the exact candidate and artifact.

- [ ] **T08 · Promote a stable production release** — Deferred / explicit decision.
  - Evidence / current state: Intentionally not performed. Your instruction is beta first, stable only after your acceptance.
  - Remaining / boundary: Retain the candidate evidence and wait for your explicit promotion instruction.

- [ ] **T09 · iOS, OTA and parallel isolated CLI host installs** — Deferred / explicit decision.
  - Evidence / current state: Not delivered by the Android/npm channel work; explicitly outside the first rollout.
  - Remaining / boundary: Keep these visible as future work, without treating Android validation as iOS validation.

## Terminal and tools

- [ ] **T10 · Keep the shell usable when an agent exits** — Implemented / verification pending.
  - Evidence / current state: Merged #222: follow the same live pane from agent to shell to next agent. Existing terminal/catalog flows passed 14/14.
  - Remaining / boundary: Recheck the reported phone symptom; subsequent connection flapping is still open below.

- [ ] **T11 · Stop repeated Connected / Reconnecting flapping** — Implemented / verification pending.
  - Evidence / current state: Two real transport races reproduced and corrected; fixed existing flow8/8. Fresh7ce full and ordinary live runs connected, with cleanup and source freeze verified. Graphics zoom followup removes its unnecessary reconnect.
  - Remaining / boundary: Sustained physical-phone network behavior remains beta acceptance.

- [ ] **T12 · Avoid black frames / flicker while scrolling Terminal Browser** — Implemented / verification pending.
  - Evidence / current state: Final normal-build Code flow: 355 frames / 52.9218 seconds, zero uniform blanks (code-screensfix-normal). Browser functional flow: 127 frames / 47.2915 seconds, zero uniform black frames (browser-screensfix-normal). The failing baseline remains 27 blank frames out of 50, maximum run 12. Scoped replacement/deletion handling and native copy-seeded resize remove the demonstrated muxr black-flash paths.
  - Remaining / boundary: One white navigation frame remains in Browser. A temporary observer measured an incoming terminal-browser Kitty image at RGB [255,255,255], per-channel spatial standard deviation 0, before transport encoding (browser-screensfix-source-pixels.json). This is upstream source paint, not a muxr-generated blank; legitimate images are not filtered. Physical-phone feel and release-mode gate acceptance remain separate.

- [ ] **T13 · Fix Terminal Code gray screen / app crash** — Implemented / verification pending.
  - Evidence / current state: The pre-existing react-native-screens 4.22.0 startup race is patched from upstream PR 4413, with ownership-safe listener install/invalidate. Historical proof: 40/40 normal, 30/30 source-instrumented ASan starts, then 40/40 final release starts. The new action-layout checks exposed a separate gray/stale surface: a background Herdr tab did not render after host restart. Controlled attachment now selects its pane, and only the visible native terminal route retains control. Main verified background host-restart recovery, return from Files, and that an offscreen Code route does not steal Browser focus on app resume.
  - Remaining / boundary: No instrumentation is shipped. The final action-layout candidate built and PASSed its complete suite, the full release gate and 40 of 40 release cold starts on the exact APK. The unresolved root-reload JNI abort stays a separate carried limitation, and sustained physical-phone behavior remains unproven.

- [x] **T14 · Make the terminal keyboard deliberate** — Done, within stated scope.
  - Evidence / current state: Merged #222: Android auto-open defaults off, explicit keyboard button and saved preference. Emulator text/graphics IME checks and real Code folder taps passed.
  - Remaining / boundary: Retain coverage when moving the toolbar.

- [x] **T15 · Reduce Tools to installed application launchers** — Done, within stated scope.
  - Evidence / current state: Merged #222: generic enabled-plugin global actions; current Browser and Code stay, ordinary shells move to Panes. Actual catalog and 80-shell fixture checks passed.
  - Remaining / boundary: New providers use the same declaration contract; no mobile provider ID list.

- [x] **T16 · Open Terminal Browser and Terminal Code for you** — Done, within stated scope.
  - Evidence / current state: Both were opened in user-visible panes; release workflow explanation was provided.
  - Remaining / boundary: Opening a tool does not close the separate rendering/crash bugs.

- [ ] **T17 · Show shell names instead of calling everything Shell** — Implemented / verification pending.
  - Evidence / current state: Implemented and pushed in #224: Herdr label/title, then folder fallback; Shell remains the type. Existing flow checks passed.
  - Remaining / boundary: Verify Home, Spaces, terminal header and tabs on the new APK.

- [x] **T18 · Move or hide terminal controls that cover the browser header** — Done, within stated scope.
  - Evidence / current state: Pane actions are anchored below the complete header chrome under the top-right three-dot button. The movable Command puck opens one anchored 268dp panel: view commands and a dark Close on the top row, then a divider and full-width Changes, Files and Tools rows, every target at full size with no common row hidden behind scrolling. Verified at stated scopes: command-panel-b-compact-smoke.json passes a real IME open and then keyboard visibility false after the panel opens at 320dp and 1.3 font scale, with every action row at least 115px at density 2.625; command-panel-b-final-web.json with command-panel-b-web-final-320.png measures the web panel at exactly 268px, Close at 44×44 and every row at 44, with real Close/Files/Tools taps navigating and zero errors; command-panel-b-final-native-dark.png, command-panel-b-final-desktop.png and command-panel-b-final-desktop-dark.png cover native dark and web desktop 1280×900 in light and dark, the dark panel measured at RGB 30,32,36, with appearance restored to Adaptive. The lead’s visual-fidelity closure matches concept B in metrics, glyphs, palette and count unit, large-font labels wrap without hiding rows, and the short-viewport fallback is verified with keyboardAfterPanel=false and every target at full size. The full gate r1 PASS exercises the release-mode text and graphics native keyboard and the closed-puck drag on the exact APK.
  - Remaining / boundary: Physical-phone acceptance stays separate. Pre-fidelity runs and the failed before-native-fix copies are retained in the lead watchers as history, not as current state.

- [ ] **T19 · Replace the Tools dropdown with a half-circle expansion** — Implemented / superseded by the requested grouped layout.
  - Evidence / current state: The user's newer request replaces the mixed Session actions fan with a movable command panel and header pane menu. Changes, Files and Tools open directly; Git history/Usage, pane controls and destructive close are separate groups. Shells correctly say Close pane rather than Stop agent. Native screenshots: terminal-quick-actions-live-code.png and pane-actions-final.png.
  - Remaining / boundary: Finish the frozen release gate before closing the revised interaction. Placement is manifest-declared through quickAction, with no bundled plugin-id list.

## Realtime personal assistant

- [x] **T20 · Restore provider settings and Codex as the default choice** — Done, within stated scope.
  - Evidence / current state: Merged #215: saved selection preserved, one provider picker, Codex login setup has no API-key prompt. Provider/module checks and real settings switching evidence passed.
  - Remaining / boundary: This proves configuration UI, not successful streaming audio or entitlement.

- [ ] **T21 · Make default Codex voice work end to end** — Implemented / verification pending.
  - Evidence / current state: Native streaming path retained. The real Codex WebRTC provider path (gpt-live-1-codex + GPT-5.6-Sol) actually passed with synthetic speech, recorded remote Opus audio and a clean spoken stop in the isolated fixture.
  - Remaining / boundary: Service-before-mic, real account handshake on the physical phone and sustained audio remain unproven; do not infer phone parity from the live-provider fixture scope.

- [x] **T22 · Select a currently live voice target** — Done, within stated scope.
  - Evidence / current state: Merged #219: fresh session list/tree intersection, stale remembered routes filtered, malformed nested data fails closed. Focused flow 8/8 passed.
  - Remaining / boundary: Keep live-route membership in future voice acceptance checks.

- [ ] **T23 · Use fuzzy lookup, focus, recent panes and full prompts** — Implemented / verification pending.
  - Evidence / current state: Merged #221: shared work inventory, fuzzy matching, phone recent views, focus/navigation, prompt targeting and initial instructions. Provider 11/11 and realtime/dictation 15/15 passed.
  - Remaining / boundary: Real model behavior still needs acceptance; scripted dispatch does not prove natural speech always chooses the right tool.

- [x] **T24 · Prompt a confirmed agent from realtime voice** — Done, within stated scope.
  - Evidence / current state: Integrated Codex natural-language handoff through GPT-5.6-Sol over a real Codex WebRTC session (gpt-live-1-codex). A synthetic spoken request caused 3 same-turn handoffs but 0 prompts and one clarification; the spoken confirmation "Yes, Jane. Please send that message" caused exactly ONE session.prompt to the isolated Jane fixture, the assistant spoke the queue confirmation, and remote Opus audio was recorded. Spoken "Go to sleep" produced realtime.closed and provider exit 0. Provider and graphics focused flows pass 13/13 (host further changes pending). Evidence: voice-summary.json, voice-confirmed-after-fix.json, voice-confirmed-remote-audio.webm and voice-spoken-stop.json under local-verification-d8da4762 pane attachments.
  - Remaining / boundary: Physical phone and service-before-mic behavior are unchanged and not proven. Installed host still has the older plugin.

- [ ] **T25 · Explain current work and PRs instead of repeatedly asking for a name** — Implemented / verification pending.
  - Evidence / current state: Committed bf2ab490: shared backend work-context lookup preserves the named target and accepts expanded context. Existing actual provider flow 11/11 passes named PR follow-ups and redaction.
  - Remaining / boundary: Natural spoken/model behavior still needs acceptance; this does not invent PR data absent from available work output.

- [ ] **T26 · Broader personal-assistant / Herdr tool coverage** — Not done.
  - Evidence / current state: Some navigation and delegation tools exist; unrestricted Herdr parity and destructive agent actions are not delivered.
  - Remaining / boundary: Inventory missing actions, implement generic capabilities and appropriate action rules, test natural requests without exposing internal IDs.

- [ ] **T27 · Actually stop when told Go to sleep** — Implemented / verification pending.
  - Evidence / current state: Merged #222: polite stop requests close the shared provider stream; negations and requests addressed to others stay open. Provider/RPC checks passed; fix installed in local host. A real spoken "Go to sleep" in the Codex WebRTC session produced realtime.closed and provider exit 0 (voice-spoken-stop.json).
  - Remaining / boundary: Confirm an actual spoken stop ends mic/audio on the phone; do not infer this from the isolated-fixture live session.

- [x] **T28 · Keep behavior in generic kernel/backend contracts** — Done, within stated scope.
  - Evidence / current state: Current Tools discovery and shared voice policy/dispatch follow this direction; native speech-to-speech remains intact.
  - Remaining / boundary: Apply the same rule to pending context/tools work. A newly added provider still needs contract and live transport validation.

- [ ] **T29 · Keep the running local host aligned with the tested app** — Implemented / verification pending.
  - Evidence / current state: Last verified deployed host is packaged source 840708b0; it is not every later main/UI commit. New settings fixes read the actual native version.
  - Remaining / boundary: After beta publication, verify/update the intended host and report exact host/app identities together.

## Attachments, viewers and settings

- [x] **T30 · Zoom and pan image attachments** — Done, within stated scope.
  - Evidence / current state: Committed in #224. Exact APK 250a8803 focused emulator flow passes real image upload, thumbnail pixels, preview, zoom, pan, fit and close.
  - Remaining / boundary: Pinch/double-tap are implemented; the automated pixel flow exercises explicit zoom and drag. Physical-phone feel remains acceptance work.

- [x] **T31 · Show selected attachments as image thumbnails** — Done, within stated scope.
  - Evidence / current state: 7ce polish gate passes actual image upload, composer thumbnail pixels, preview, magnify/pan/fit, remove and Settings checks.
  - Remaining / boundary: Publish the final ARM64 beta to make it available on the phone.

- [ ] **T32 · Render rich attachment formats inside the app** — Implemented / verification pending.
  - Evidence / current state: Integrated 8105a626: bounded offline Markdown/Mermaid, PDF, CSV/XLSX and sanitized HTML/SVG. Browser flow passes nonblank PDF/page navigation, worksheet switching, diagrams and remote-content blocking; mobile typecheck and existing attachment flow 4/4 pass.
  - Remaining / boundary: Fresh native build and actual attachment-row-to-preview device acceptance remain required.

- [ ] **T33 · Make version mismatch obvious and Settings easier to navigate** — Implemented / verification pending.
  - Evidence / current state: Implemented/pushed #224: actual native version/build replaces stale Expo 0.1.12; Home and top Settings notice link to one Connection & updates page. Duplicate summaries removed.
  - Remaining / boundary: Verify true mismatch visibility, no false same-release beta mismatch, diagnostics and one-tap navigation on device.

- [x] **T34 · Improve code/diff/document reading and line links** — Done, within stated scope.
  - Evidence / current state: Merged viewer work: pan/zoom, narrow controls, CJK row extent, source line labels, real diff route, cold/warm line 200, bounded offline error and recovery. Matching local emulator gates passed.
  - Remaining / boundary: Some individual visual details, including equal-count word marking and full scrub label proof, remain less directly evidenced than the main flows.

## Usage, testing and review

- [x] **T35 · Fix Usage providers and recently used ordering** — Done, within stated scope.
  - Evidence / current state: Consolidated Usage implementation and private-context policy reviewed. Actual implementation fixture flow OMP 150 → OpenCode 300 → OMP 150, selected tabs and recency passed. The fixture now asserts against the real clock again and its focused check passes.
  - Remaining / boundary: Go unauthenticated guidance was checked; no fabricated Go account quota or every-provider live-account claim.

- [x] **T36 · Create strong local end-to-end and performance gates** — Done, within stated scope.
  - Evidence / current state: Merged local-only harness: bounded phases, strict mounts, CPU/PSS/frame counters, native cells/framebuffer evidence, APK pullback SHA/native provenance, failure artifacts and emulator ownership.
  - Remaining / boundary: The prior matching full gates passed; every new relevant mobile build needs its own matching acceptance.

- [x] **T37 · Remove emulator-specific GitHub workflows** — Done, within stated scope.
  - Evidence / current state: Removed as requested. GitHub runs normal CI/build/release work; device testing stays local.
  - Remaining / boundary: Keep local evidence linked to exact source and artifact identities.

- [ ] **T38 · Test all current polish before finalizing the PR** — Implemented / verification pending.
  - Evidence / current state: Historical gates retain their actual identities. V3 source a685ab58 builds and passes all 34 suite checks and the full native gate, including both corrected toolbar drags (303px each), keyboard/menu separation, Changes, graphics and all four measured performance phases. Initial-paint ordering and pending-open cancellation are corrected; real-wire, existing transport flows and native Code-return evidence are retained. The newer arrow/file-count UI and cached hunk navigation pass through Metro without rebuilding.
  - Remaining / boundary: Finish the corrected final-candidate full, Changes, rich-preview, polish and cold-start checks. Low-thinking GPT-6 Astra workers now run delegated verification under explicit user authorization; Main integrates changes. Human voice on the physical phone and sustained phone scrolling are not inferred from local or provider-fixture evidence.

- [ ] **T39 · Independent PR review, comments and final evidence** — Implemented / verification pending.
  - Evidence / current state: Independent Standards and Spec reviews completed. The private-state matrix was replaced by one public Unix-socket flow; the native invalidation, graphics handoff and allocator findings are corrected and reviewed closed. Main owns integration; the user's later instruction permits delegated native verification and a new action-contract review. Actual Code acceptance and recordings are retained. The updated 16-feature PR narrative is prepared locally, not published.
  - Remaining / boundary: Finish final-candidate validation and obtain authorization to commit/push or publish the final narrative. Exact-head remote CI and any merge/release follow only after that decision.

## One-tap mismatch repair

- [ ] **T40 · Tap mismatch to keep the app and select a compatible host or app update** — Implemented / verification pending.
  - Evidence / current state: Implemented exact-release managed Linux host alignment with confirmation, protocol/state compatibility, preserved beta identity, private snapshot, restart health/rollback and interrupted-job recovery. Standards and spec reviews pass. Real installed-package flow passes update, rollback, custom data directory, ownership and interrupted recovery.
  - Remaining / boundary: Native Settings entry and final packaged beta still need delivery checks. Compatible version differences do not require repair; missing supported metadata leaves alignment unavailable. No real host changed by the test.

## Latest reported regressions

- [x] **T41 · Start Code / Browser must create and open a pane** — Done, within stated scope.
  - Evidence / current state: Main used the new native Tools quick action to create and open both actual applications through an isolated source-plugin catalog. Browser rendered the owned tracker document; Code rendered the current worktree explorer. Evidence: terminal-quick-browser-live-document.png and terminal-quick-code-launch-final.png. Installed plugin registrations were not changed.
  - Remaining / boundary: The local installed host has since been updated with your explicit authorization to 0.1.26-dev.local.20260906.62b4e99d, which delivered the next realtime message successfully. Merge and beta publication remain held for your decision.

- [x] **T42 · Preserve ordered graphics updates on mobile** — Done, within stated scope.
  - Evidence / current state: Committed b05b8835: reproduced dropped inline placement in actual write pump, replaced latest-only storage with bounded FIFO and explicit recovery. Existing flow baseline 6/7 then fixed 7/7; native APK 250a8803 full emulator gate passed.
  - Remaining / boundary: This closes the demonstrated dropped-frame bug; it does not establish the physical-phone black flicker or crash is fully resolved.

## Workspace cleanup

- [x] **T43 · Clean obsolete Herdr spaces and shells** — Done, within stated scope.
  - Evidence / current state: Closed 46 confirmed obsolete panes: 34 idle shells, 8 completed agents and 4 old demos. Workspace count 24→8; tab count 65→26. Recoverable inventory archives are attached.
  - Remaining / boundary: Preserved active conversations, current Browser/Code demos, build shells and emulator. No source worktree or saved transcript was deleted.

## Changes and current interaction polish

- [x] **T44 · Show the actual worktree and comparison in Changes** — Done, within stated scope.
  - Evidence / current state: Historical b4b3bc38 focused Changes gate and full native gate pass worktree selection, Working/Staged/Branch patches and preserved session context; Screenshot29138 showed the original checkout, not the release worktree. For the V5 candidate, Grounded in perf/prGate.mjs (lines 663-672), the full document feature phase calls viewerControls(), changesScopeControls() and viewerLineTarget(), so a passing full gate already executes the exact standalone Changes flow and emits report.changes with branch/working/staged screenshots: standalone Changes is not a separate unmet acceptance after a full PASS and no duplicate run is needed. The r1 full gate PASSes on the exact APK and emits that report.changes with its branch, working and staged screenshots and preserved session context, so this is carried by a real passing run rather than by inference.
  - Remaining / boundary: Physical-phone acceptance remains separate. Select the intended registered worktree and comparison explicitly.

- [x] **T45 · Make graphics Zoom actually magnify and pan** — Done, within stated scope.
  - Evidence / current state: Final actual Code pass preserves 1.25x zoom through repaints, and a real injected two-finger pan runs with no Code viewport change, alongside file/folder clicks, separate scroll targets and IME open/close (code-final-observed evidence).
  - Remaining / boundary: Physical-phone zoom/pan feel and sustained scrolling remain acceptance. The reproduced startup SIGSEGV is fixed and separately verified under T13.

- [x] **T46 · Scroll Code editor and Explorer at the finger location** — Done, within stated scope.
  - Evidence / current state: 7ce real native Code test: sidebar selection then editor swipe changed first rendered line5 to10 without extra editor focus; sidebar swipe moved file rows while editor stayedline10. Exact screenshots retained.

## Fast development feedback

- [x] **T47 · Run the app from Metro instead of rebuilding release APKs for every edit** — Done, within stated scope.
  - Evidence / current state: Visible Fast Refresh, host/watch error preservation, renderer watch, no-build reopen and owned-process cleanup are proven. Actual dev-client terminal attach now passes with 9–52ms first frames after client and host use matching normal relay tickets. The managed checkout-plugin development path is verified by source-plugin-runtime-smoke.json: nine registered bundled plugins load through checkout paths, registration and enabled states and unrelated plugins are preserved, terminal sibling linkage is automatic and connects, permissions are private, and closing the runtime removes it. The dev supervisor's socket lifecycle and shutdown are repaired, browser-asset preparation runs at startup, and its relay now allows the native origin plus both Metro origins; the fix is verified by an actual native reconnect. Web verification was repeated after the allowlist fix: the 320dp web panel passes Files/Tools rows and navigation with zero errors, on the pre-fidelity UI; a post-fidelity web repeat stays pending.
  - Remaining / boundary: Use yarn dev and yarn dev:android --no-build for daily work; rebuild the debug client only after native changes. Isolated state/ports preserve the production app and service. This is not proof of production pairing or continuous graphics rendering.

## Command control identity

- [x] **T48 · Use a Command-key icon rather than a Settings-like control** — Done, within stated scope.
  - Evidence / current state: Changed the shared native/web terminal puck from options-outline to the Command-key glyph. Actual dev-client screenshot confirms the distinct Command symbol after Fast Refresh, without rebuilding the APK. Open/close, drag target and command behavior are unchanged.
  - Remaining / boundary: The translucent top-right gear is Expo development-menu chrome, not the app control and not present in the production APK. Superseded on device review: the Command glyph read as visually dominant and semantically wrong on Android, so the closed puck now carries the app's own overflow icon on a 38dp disc inside its unchanged 44dp target. Do not restore the Command glyph.

## Stable document navigation and change totals

- [x] **T49 · Keep up/down diff arrows visible and separate the file count** — Done, within stated scope.
  - Evidence / current state: The existing Metro client shows a separate File N of M label above fixed 44dp controls. Arrows persist in file mode and diff mode, and a 320dp viewport retains file/hunk navigation with zoom available in the menu. Live inspection reproduced a second defect: a cached file's 17 hunk positions were erased by the parent reset after child publication. Reset ordering is corrected; cached next/previous file navigation and both hunk jumps now work without an APK build.
  - Remaining / boundary: Final release acceptance will use the settled source; Metro proof is not relabeled as a new release APK.

- [x] **T50 · Show green additions and red deletions beside Changes in Command** — Done, within stated scope.
  - Evidence / current state: Generic bounded summary metadata flows from the plugin to the shortcut. Actual Git execution reports +100 / −50 across 50 modified tracked-text files, plus one binary and one untracked file: badge 52, independent of the 49-file page. Totals describe tracked numeric line changes; binary and untracked contents are not fabricated as line counts. The selected concept B panel renders the signed green and red totals trailing the Changes label with the plain count on a secondary line, and the full gate rerun emits report.changes with its branch, working and staged screenshots on the exact APK.
  - Remaining / boundary: Binary and untracked files stay counted as files rather than lines, by design. Physical-phone acceptance stays separate.

## Verification and update rules

- Keep historical failures alongside reruns; never reuse old APK evidence for changed mobile/native bytes.
- A passing emulator or provider fixture does not prove physical-phone rendering, account entitlement, real speech or remote audio.
- Update this checklist when an item changes state and link the relevant commit, PR or artifact. No new test matrix or agent swarm is required.
- Historical full/Changes/rich/attachment gates remain pinned to their original source and APK. Current Code click/zoom/scroll/pan/IME acceptance passes on the normal fixed APK, and the reproduced startup crash passes normal and sanitizer reruns. ARM64 beta delivery remains held for the user's merge/release decision.
- Current verification: Code 355 frames with zero uniform blanks; Browser 127 frames with zero black frames and one proven source-origin white frame; 40 normal plus 30 ASan restored starts pass; workspace typecheck and 197 Vitest tests pass. A fresh test-signed release APK is built; the release-mode full gate and complete local suite are still running/pending. Prior live Codex speech-to-speech proof remains limited to the isolated confirmed-agent fixture; physical-phone voice/audio is not claimed.

## Links

- [Current consolidated polish PR](https://github.com/umeranjum17/muxr/pull/224)
- [Merged terminal polish / channel workflow](https://github.com/umeranjum17/muxr/pull/222)
- [Merged release cache fix](https://github.com/umeranjum17/muxr/pull/223)
- [Failed first beta run, retained](https://github.com/umeranjum17/muxr/actions/runs/33952896064)
- [Installable older phone preview, build 356](https://github.com/umeranjum17/muxr/releases/tag/preview-840708b0)
- [Release runbook](../RELEASING.md)

## Revisions

- 2026-09-05: Created one session-wide tracker at the user's request, including latest thumbnails and movable-toolbar requests, evidence limits and deferred production promotion.

- 2026-09-05: Reopened Code/Browser graphics acceptance after the user observed severe black flashing on emulator file opens. Beta held; investigate replacement versus retirement and native frame continuity. Magnification alone is not Code acceptance.

- 2026-09-05: User prioritized development feedback first. Add explicit source dev-server and separate Android dev-client commands; prove visible refresh and host restart without repeated release APK builds. Beta remains held for actual graphics interaction acceptance.

- 2026-09-05: Proved visible native Fast Refresh without install/process change, source host restart, compile-error preservation, attachment renderer watch, no-build reopen and owned-process cleanup. Dev loop remains running. Independent internal Opus workers now own actual graphics interaction acceptance and lifecycle review in parallel; beta remains held.

- 2026-09-05: Main-owned parallel review found placement-only hides were treated as image destruction. Corrected scoped hide/replay/delete behavior; existing flow failed before and passes after, independent reviewer closes that blocker. Device worker separately found missing dev stream tickets; canonical local credential provisioning is implemented and awaiting native acceptance. User-requested Command-key control icon is visually verified.

- 2026-09-05: User changed execution order to coding first, then joint verification. Code integration and 13 focused local flows pass. Live/device automation is stopped. No merge, beta publication or host upgrade until joint acceptance. Codex delegated work is pinned to GPT-5.6-Sol with no 5.5 fallback; native realtime audio remains unchanged.

- 2026-09-05: The user is asleep; Main runs complete local verification of the latest checkout (d8da4762) overnight and sends code fixes to GLM Flash workers, repeating failed paths, leaving the checkout ready for merge discussion. No joint-user wait, no delegated device checks, no merge, publication or installed-host upgrade. First full suite 32/34 with a terminal input echo failure and a local untracked .omp config dirty-source seal failure; synthetic voice replay exposed duplicate same-turn message queuing, fix in progress. None of this is marked tested.

- 2026-09-05: Main verified voice end to end: real Codex WebRTC (gpt-live-1-codex + GPT-5.6-Sol) produced exactly one session.prompt to the isolated Jane fixture after a spoken confirmation, the assistant spoke the queue confirmation with remote Opus audio recorded, and a spoken "Go to sleep" closed the stream. T24 is done within live-provider and isolated-fixture scope; physical phone and service-before-mic remain unproven. Graphics stay blocked: actual file video 123/187 blank frames with zoom reset, coalescing-deletion fix being refined, pending-bitmap freeze changed to copy-seed, native cold-start Fabric crash twice under diagnosis, final build/full check rerun pending.

- 2026-09-05: Main's final actual Code interaction pass verified first-tap file opens, folder collapse/expand, 1.25x zoom preserved through repaints, separate editor/sidebar scroll, real injected two-finger pan and IME open/close with the app alive: 347 encoded frames / 52.3736s, zero uniform blank frames against a failing 50-frame baseline. Native copy-seed fix avoids blank reallocation and the old pending-surface freeze; the bridge defers current-image deletes only when a queued same-surface successor exists, flushing a real delete otherwise. Focused flows 14/14 and native patch guard pass; fresh dev cold starts 0/10 crashes. The earlier 2 Fabric SIGSEGVs keep unproven cause and are not claimed fixed. Browser-only navigation check running; clean-source full suite/release gate not rerun; beta merge/publish/host upgrade stay blocked.

- 2026-09-05: The Browser functional native flow passed (browser-final-observed): 131 frames / 47.4572s with zero uniform BLACK frames, retaining one uniform WHITE frame (frame 101, page navigation, meanRGB 253 / std 0) with PNG/video evidence; the follow-up passed 4 native page navigations with 0/23 uniform frames plus 5 direct Chromium screencast frames (none uniform) under browser-navigation-provenance, but did not establish white-frame provenance. New final reviews: Standards no findings; Spec found 4 real lifecycle defects (agent://FinalSpec) assigned to GraphicsInputFix — bounded failed-successor retirement, preserved deferred-delete semantics, surviving full/inline surface reporting, and rejection of deleted cached-image replay unless retransmitted. Latest-source verification is not complete until those land and Main reruns; the Code/browser evidence remains valid for the pre-review snapshot.

- 2026-09-05: The current native APK (b4942d7e) crash is reproduced after Code/browser activity: restored-graphics-cold-starts.json passes 4 cold starts, then the cycle-5 start SIGSEGVs at MountingCoordinator+706 (restored-native-crash.log in local-verification-d8da4762). The earlier fresh 10/10 cold starts remain historical, not resolution. Main is preparing ASan diagnosis with DevAndroid; graphics review corrections need revision (duplicate field compile blocker; true resident cache delete lifecycle). No merge readiness until fixed and verified.


- 2026-09-05: The four current graphics lifecycle findings are resolved per FinalSpec's final semantic reread; Main independently ran yarn vitest run apps/host/src/agent/infrastructure/herdrGraphicsBridge.test.ts apps/host/src/agent/infrastructure/inlineKitty.test.ts with 4/4 passed on current files. Still blocking merge readiness: an actual final-source Code/browser repeat and the native startup crash. ASan-instrumented JNI/reanimated/worklets is installed with 20 awake restored cold starts underway; one initial diagnostic startup failure was emulator asleep/Metro timeout, no native crash, and the throwaway repro was fixed to wake each cycle and verify the exact MainActivity. Full RN ASan source build setup underway.

- 2026-09-05: Main found and fixed one more graphics retirement race: a same-key candidate arriving during an awaited failed route could re-defer the deferred flush. The added existing-flow check (later route held pending; phone must receive the old delete immediately) failed pre-fix on graphics true vs false and passes with forwardInlineBlock allowDeleteDeferral=false across all 3 settled execution paths; 4/4 graphics+inline vitest, host tsc pass, and FinalSpec read clean again. ASan 20/20 awake restored cold starts pass MainActivity launches with session previews, but the original native APK b4942d7e crash cause remains unproven. The full RN source diagnostic build exposed an app build.gradle signing guard incorrectly matching release tasks from dependencies during app:assembleDebug; the graph check is now scoped to it.project==project with source-build and app release rejection verification pending.

- 2026-09-05: The user reported cybersecurity flags, then explicitly said continue, don't do anything wrong, fix app; crash work resumed with tracking only. The full source ASan build succeeded; the first full run failed on an identical double_conversion globals ODR (diagnostic-only) and the throwaway wrapper with detect_odr_violation=1 preserved size-mismatch checks; the corrected full run passed 10 starts then cycle 11 hit an ASan heap-buffer-overflow READ8, with llvm-addr2line confirming MountingCoordinator.cpp:114 — the same delegate call as the original crash. Evidence /tmp/asan-kit/full-level1-starts.json and .log. Root is NOT fixed; Main adds throwaway registration logging to source RN only, no product patch. The signing graph guard is verified (debug source RN build succeeded with dependency Release tasks and no release creds; actual app assembleRelease --dry-run still fails with expected missing signing props). The Metro watcher fix survived 200 create/delete ReactAndroid native build trees (metro-native-build-churn.json). Merge/publish/production upgrade remain held.

- 2026-09-05: ROOT CAUSE FOUND AND FIXED for the native startup crash, and it is NOT caused by our graphics changes: a pre-existing upstream react-native-screens 4.22.0 bug, fixed upstream by merged PR 4413 (b3badd012f83679b12f4e29f2e28eceaa4830efd). ScreensModule.initialize() registers its lifecycle listener before setupFabric(); when the host is already resumed, onHostResume() (UI thread) races setupFabric() (module-init thread) through the unsynchronized if (!screenRemovalListener_) check, and the racing shared_ptr move-assign leaves a mismatched {object pointer, control block} pair whose weak_ptr reports use_count=1/expired=0 while pointing at freed memory; the next pullTransaction locks it and virtual-calls through freed memory; RN core has no removeMountingOverrideDelegate, so the dangling entry is permanent. Local proof chain: throwaway source-RN registration logging showed RNSScreenRemovalListener registered as a delegate on surface 1, and ASan reported heap-use-after-free READ8 at MountingCoordinator.cpp:114 with frame #1 in librnscreens.so (delegate-trace-starts.json/.log), matching both original tombstones (MountingCoordinator+706, SEGV_MAPERR 0x600000017 and SI_KERNEL 0x0, thread mqt_v_js). Fix: patches/react-native-screens+4.22.0.patch (8.7KB, source-only: NativeProxy.cpp/.h, cpp/RNSScreenRemovalListener.cpp/.h) matching upstream — process-lifetime function-local-static listener, mutex-guarded swappable callback capturing the JNI global ref by value (never this), monotonic token so a stale proxy's teardown cannot clear a newer install, install/invalidate serialized on installMutex_, disarmed listener passes the transaction through. All throwaway RN instrumentation reverted (MountingCoordinator.cpp restored, 0 MUXR_DIAG lines). Regression guard added to the existing scripts/diagnostics/application/verifyNativePatches.mjs: 11/11 checks pass including the new screens check, no new test file. Verification so far: rebuilt normal non-ASan x86_64 APK carries setListener/clearListener in librnscreens.so; cold-start stress 0 crashes past cycle 13+/40 (previous failure points cycle 5 normal / cycle 11 ASan); ASan+fix APK staged for the deterministic rerun. Merge/publish/production upgrade still held.

- 2026-09-06: Closed the reproduced screens startup failure with 40/40 normal and 30/30 source-instrumented ASan starts. Final normal Code recording passes file/folder/zoom/independent scroll/two-finger pan/IME and has 0/355 uniform blank frames. Browser flow passes with 0/127 black frames and one white navigation frame. A scoped temporary incoming-image observer establishes source-white provenance (RGB 255, per-channel standard deviation 0); no color filter or stale-frame suppression was added. The observer is stopped. Clean test-signed release APK build succeeds; its full gate and the complete local suite remain pending. No merge, publication or production-host upgrade.

- 2026-09-06: Fresh release-mode full gate PASS, failures empty, source frozen at 367de0c1 / mobile bbab937d. Complete suite 33/34: package smoke's seal step requires an isolated clean-source fixture; production clean-source guard remains strict. Standards finding closed after replacing the newly added private-state test matrix with one real public Unix-socket protocol flow (3/3 graphics tests pass). Spec review found three additional lifecycle paths: native reinstall after invalidation and lost deferred-delete semantics during direct/registration handoffs. Reopened T12/T13 for those corrections and a final candidate rerun. Main's native null-state guard is independently reviewed closed; an extracted real callback-order reproduction fails before and passes after. Workers own graphics and package-smoke corrections; Main owns all verification. Prior artifacts retain their original identities.

- 2026-09-06: All three final lifecycle findings reviewed closed after integration. Direct image allocation now avoids live/latest IDs; the real public socket flow reproduced the collision before the fix and passes after, including neighbor-preserving replay and a controlled last-channel departure while a deleted cached successor routes. Removed the worker's unconditional current-generation cache forgetting; deferred deletion uses its original captured stamp. Restored workspace cache invalidation. Native null-state guard is terminal under the installation mutex. Package smoke snapshots only existing nonignored source plus required build outputs, then seals/verifies the real artifact in its own clean repository; real checkout/index and production release guard remain unchanged. Final candidate is frozen. Its first build attempt hit an unconfigured keytool shim; the failed log is retained and the throwaway runner now pins the already-installed Java 21 toolchain, without changing user configuration.

- 2026-09-06: The final crash/graphics candidate passes clean release build, complete local suite 34/34, full emulator gate and 40/40 restored release starts (source 0b8e391d, mobile ce3537ec, APK cd5de58b). Normal Dev APK rebuild also passes. A native Tools probe initially used the installed 840708b0 plugin; the isolated source-plugin catalog then successfully created and opened actual Code without changing installed registrations. Browser launch validation is still pending. The user requested a top-right three-dot pane menu plus direct grouped floating quick actions, reopening T18/T19 before merge discussion.

- 2026-09-06: Implemented the requested top-right pane menu and direct grouped command shortcuts; Main verified native Changes/Files navigation, both application launches, menu separation and compact scrolling. The Files flow exposed a mutable React ref captured by a swipe worklet; removed it and proved two sequential folder pops with zero new worklet warnings. A distinct background-Code graphics failure is fixed through authorized control focus plus visible-route-only stream ownership, verified across native return, offscreen app resume and host restart. A popup-dismissal automation coordinate overlapped the destructive row; its confirmation was canceled without closing anything, and the gate now dismisses via Android Back. New frozen release build started; earlier candidate hashes/results remain historical.

- 2026-09-06: The first new suite found a shell-label declaration-order error; corrected it, rebuilt an immutable candidate and passed all 34 suite checks. Candidate source 0bccd7e3 / mobile 020f86de / APK bb9314f0. Retained the failed first-candidate evidence. Ordinary yarn dev is restored, the source-catalog proxy and headless artifact browser are stopped, and installed registrations remain untouched. Native-gate capability assumptions and the later blank Code return are under verification, not waived.

- 2026-09-06: The user explicitly authorized lower-thinking GPT-6 Astra delegation for testing. Two workers are active: exclusive native-gate ownership and independent action-contract review. Main's isolated real TerminalManager/Herdr probe observed cached graphics before the initial ESC[2J text paint on both reattachments (55 ms and 85 ms later). Initial-paint ordering is now a tracked correction; no claim that the later blank return is resolved yet.

- 2026-09-06: Corrected initial-paint ordering in the real host: pre-paint resize retains geometry but cannot register graphics, and another bridge opening cannot replay to an unpainted stream. Existing ordered-backpressure flow: 3/4 before, 4/4 after. Real-wire corrected returns deliver graphics after the initial text paint (+9 ms and +1 ms). Native clean Code returns and pointer input pass. The older diagnostic fixture separately showed upstream page zoom 0.314; its installed producer was not modified, and it is not substituted for clean native-path evidence. Astra traced the drag failure to the previous text drag's persisted clamp and corrected the generic harness destination. V3 frozen acceptance is delegated; Main keeps integration ownership.

- 2026-09-06: V3 build, 34/34 suite and full native gate pass with stable source/APK identity; rich/polish/standalone Changes/cold starts were not run because the candidate was superseded. The user requested persistent up/down arrows, a separate file count and colored Changes totals, then correctly identified repeated release builds as a workflow bottleneck. Main reopened the existing dev client with --no-build, verified the new navigation layout and fixed cached-file hunk reset ordering through Fast Refresh. A managed checkout-plugin development path is being added without changing installed registrations. Further release builds are held until interactive changes settle.

- 2026-09-06: The user rejected the horizontal action layout after Main visually checked Fable's three concepts against Metro screenshots and selected concept B, the anchored list; a GLM 5.3 Flash built-in subagent implements it and the result is neither accepted nor verified. Muse 1.3 coding agents were blocked by a provider 403 DataPolicyError requiring contributor-data opt-in; no opt-in happened, so built-in GLM subagents do the coding while Main keeps management, review and all verification. The managed checkout-plugin development path is proven by source-plugin-runtime-smoke.json: nine registered bundled plugins load through checkout paths, registration and enabled states and unrelated plugins are preserved, terminal sibling linkage is automatic and connects, permissions are private, and close removes the runtime. The 52-file Changes scenario and the historical V3 34/34/full native gate results stand as recorded. Release remains held: no new APK, merge, publication or installed-host upgrade, and rich, polish and final release acceptance stay open.

- 2026-09-06: Concept B is implemented and reviewed rather than pending: GLM 5.3 Flash developers built it through visible OpenCode panes (one anchored 268dp panel, view commands and dark Close on top, divider, full-width Changes/Files/Tools rows with signed trailing totals and a secondary count, bounded scrolling under the pinned Close, no plugin IDs, no horizontal carousel, 44dp targets), and an independent Opus front-end review drove the corrective passes for the missing web Close row, real bounded panel scrolling, derivation of panel position without disturbing the dragged puck, compact-type cleanup and metadata/label readability bounds. On the pre-fidelity revision the lead's native behavioral smoke passes the normal, 320dp/1.3-font and short keyboard viewports, and web 320dp navigation passes; only that scope is retained. Exact visual fidelity to the selected concept (rounded-square Close, spacing, icon order, typography, count unit) is under Opus revision before freeze and remains pending lead verification, not accepted, and exact B further requires that common actions stay visible without hidden scrolling in a short viewport, with Opus's fallback pending. The isolated source dev supervisor is healthy again: its startup shutdown, web asset preparation and the natural-relay plus both Metro origins allowlist are in place and the fix is verified by an actual native reconnect, with the web 320dp repeat passing after the allowlist fix on the pre-fidelity UI; post-fidelity repeats and the single final release pass still pending. The Usage fixture asserts against the real clock again and its focused check passes. Old V3 results remain historical at their original identities; no fresh APK, merge, publication or installed-host upgrade has happened.
- 2026-09-06: Coordinator and model ownership reconciled ahead of the final freeze: the lead coordinates and owns acceptance, Opus owns the final frontend, GLM 5.3 Flash owns native/dev plumbing and documentation, and Codex owns all device and web verification; the historical Next-execution-order text that delegated native gates to GPT-6 Astra workers now reads as history rather than current authority. The final source is freezing and a dev APK native hideKeyboard build is running with no result yet; statuses stay implemented/verification-pending and nothing is claimed tested before fresh final results. Preserved as qualified evidence rather than release-proven failure or dev-only claim: the current compact-view before-fix native report (superseded UI revision) and the dev Reanimated hot-reload crash (dev loop only).

- 2026-09-06: Fresh UI verification recorded at stated scopes only: dev-hide-keyboard-build.json PASSes the native debug hideKeyboard build at 09:43:49Z; command-panel-b-compact-smoke.json passes the real IME opening (keyboard true) and then keyboard-before-panel false at 320dp/1.3 font with all rows at least 115px at density 2.625, retaining the failed before-native-fix copies; command-panel-b-final-web.json plus command-panel-b-web-final-320.png pass the post-fidelity web panel at exactly 268px with Close 44×44, every row 44, real Close/Files/Tools navigation and zero errors. The native-normal rerun automation hit a navigation-timing interruption and its final result is under investigation. No final release has started and full ready-to-merge is not asserted.

- 2026-09-06: The final source is frozen and release build V5 is running; its build, complete suite, full gate, rich, polish, changes and cold-start results are still pending and nothing is asserted ready. Fresh appearance verification recorded at stated scopes: command-panel-b-final-native-dark.png passes the native dark panel, and web 1280×900 light plus dark pass as command-panel-b-final-desktop.png / command-panel-b-final-desktop-dark.png with the dark panel at RGB 30,32,36 as expected; both appearance settings were restored to Adaptive. Files return plus Tools repeated with a 1.2s transition settle passed without source changes, and the smoke JSON records the earlier interruption alongside the completed repeat.

- 2026-09-06: Release build V5 PASSes at 09:52:29Z on the frozen final source: workspace source digest 62b4e99d134f2824c2f46af9c594e1a4e8746dcc30f658eb9021fbd9aaa77874, mobile digest 3ed846cc43f9880226bdee8ed5d0cbedd0797db027e2e819c4198e69bbc2bb39, APK SHA-256 b27870b6c6f98316a63fc2483881d7fa68679d8c04b643b1da6e6e8112ef1d81; provenance pane-actions-final-release-v5-provenance.json is copied to both lead watcher roots. It is the x86_64 release variant with the test signer, not the phone artifact or a publication. The complete local suite is running; statuses stay verification-pending until the actual suite, full gate, rich, polish, changes and cold starts land.

- 2026-09-06: Lead visual-fidelity closure confirmed: command-panel-b-final-normal.png together with dark and 320dp/1.3-font screenshots taken with a real keyboard matches the selected concept B in metrics, glyphs, palette and count unit, large-font labels wrap without hiding rows, and the no-hidden-short-viewport fallback is verified with keyboardAfterPanel=false and all targets at full size. The overdue pending items for these scopes are closed in this reconciliation; the release suite and full gate remain pending and statuses stay verification-pending.

- 2026-09-06: Suite closed: V5 local suite PASSes 34/34 in 172.1 seconds, finished 09:55:50Z (pane-actions-final-release-v5-suite.log and -result.json in the lead watcher roots). The full release emulator gate has started; rich, polish, changes and cold starts remain after it. A read-only teardown-crash investigation confirms a structural risk rather than a dev-only one. Attribution-final read-only report dev-fidelity-crash-analysis-v3.md (lead watchers) verifies the upstream 325a644e/#9789 remote-function lifetime fix does not guard our getAnimationTimestamp callback signature; no safe matching backport is identified in our investigation and no patch is applied. The single observed dev root-reload JNI abort stays unresolved; the final gates and cold starts will bound tested behavior but must not be claimed to prove the race absent. Known limitation for the release: rebuild-triggered root reload can reproduce this JNI abort in the local WebGL/native runtimes; product release builds do not use the dev root-reload path, yet the race is not proven absent from those builds. Statuses stay verification-pending.

- 2026-09-06: Crash attribution final read-only report dev-fidelity-crash-analysis-v3.md lands in the lead watchers, and its conclusion is carried into the current state rather than only the history appendix: upstream 325a644e/#9789 (remote-function lifetime fix) does not guard our getAnimationTimestamp callback signature, no safe matching backport is identified in our investigation, and no patch is applied. The single observed dev root-reload JNI abort stays unresolved; the final release gate and cold starts will bound tested behavior but must not be claimed to prove the race absent.

- 2026-09-06: V5 full gate first run FAILs on the graphics checkerboard only; every other phase and feature passes, including the standalone Changes coverage, text IME/drag and usage (full/report.json and graphics-pixels.png retained in the lead watcher roots). The lead's read-only diagnosis pins the checkerboard to the harness fixture's first pane while a coordinate tap under live card churn selected another working pane (its screenshot shows a positively resizing terminal). The harness-owner GLM is confirming and fixing deterministic proof-pane selection in the harness only — no product or APK change is indicated, the full gate is not labeled passed, and the exact APK is rerun after the harness fix.

- 2026-09-06: Graphics harness fix landed and reviewed, harness only: the text phase opens the pinned proof pane through its real persisted session binding (shared with the line-target route resolution) instead of a fixed coordinate tap, then asserts the mounted pane header and a fresh positive-cell-dimension terminal.resize for that exact pane id before the graphics phase samples pixels; observer thumbnail reads carry no cell dimensions and are no longer accepted as attach proof, and the checkerboard thresholds are unchanged. The stale flow comment claiming the producer follows whichever pane the phone attaches to is corrected. No product, mobile or APK bytes change; the exact APK reruns and the full gate stays unpassed until it does.

- 2026-09-06: Runtime-reload boundary recorded beside the crash analysis: dev-fidelity-crash-runtime-boundary.md (lead watchers) shows logout/reset calls Updates.reloadAsync and the installed expo-updates DisabledUpdatesController restarts the React application through RecreateReactContextProcedure even though expo.modules.updates.ENABLED=false, so an app-accessible runtime reload path exists in release configuration. Ordinary activity lifecycle destruction is a distinct path. This is a candidate trigger, not an observed release reproduction; the crash is not labeled dev-only, no matching patch is applied, and passing cold starts do not prove the reload race absent.

- 2026-09-06: V5 full gate rerun r1 PASSes at 10:33:53Z with no failures, on the same APK and source bytes as the failed first run (pane-actions-final-release-v5-r1-full/report.json in both lead watcher roots): all four performance phases, the persistent viewer with file, hunk, line-200 and offline recovery, Changes branch/working/staged, text and graphics native keyboard with the closed-puck drag, and usage. The corrected proof pane resolved to w1:p1 with header · 1/4 and fresh 19×37 cell-pixel resizes, and the checkerboard passed before and after the native keyboard resize, confirming the original failure as fixture selection rather than a product defect; the original failing run is retained. Because the full gate's document phase runs the standalone Changes flow, no duplicate Changes run is outstanding. Rich, polish and 40 cold starts remain.

- 2026-09-06: Attachment polish flow PASSes at 10:39:23Z with no failures, on the same V5 APK and source bytes and the frozen harness (pane-actions-final-release-v5-polish/report.json in both lead watcher roots): installed version 0.1.26 reaching settings, composer upload, thumbnail, preview, real pixel zoom and pan against their movement thresholds, and removal. Feature scope only; the performance phases stay with the full gate. 40 cold starts remain.

- 2026-09-06: Local acceptance sequence closed. Release cold starts PASS 40/40 at 10:45:49Z with zero failures and no new crashes on the exact V5 APK (pane-actions-final-release-v5-cold-starts.json and .log in both lead watcher roots), and final-acceptance-sequence.json records code 0 for all four jobs: full-r1, rich, polish and cold starts. Scope is the normal release APK, not ASan and not physical-phone acceptance. Statuses now reflect their stated scope: T18 and T50 are done within scope. Open and unchanged: your physical-phone acceptance, the unresolved root-reload JNI abort with its app-accessible runtime reload path, and the merge, publication and installed-host upgrade decisions.

- 2026-09-06: Release-runtime reload reachability observed rather than inferred: a Settings logout on the owned V5 release build reached the pairing screen in 14 seconds under the same process id with no new fatal signals, and its ReactHost logs show the real reload task, the RecreateReactContextProcedure restart, the ReactContext and ReactInstance being destroyed and surfaces restarting (release-logout-smoke.json and .log with before and after screenshots, both lead watcher roots). The dev root-reload JNI abort did not reproduce in that single logout, which is not race stress and does not prove it absent; the abort stays unresolved and no matching patch is applied.

- 2026-09-06: The root-reload JNI abort has a matching merged upstream fix after all, superseding the v3 attribution above rather than erasing it. Upstream PR 10278 (b3157cd97388554174449cbb5466944749618747) stops the AnimationFrameQueue when WorkletsModule is invalidated; it is backported to the installed Worklets 0.7.2 as Java, since upstream implements it in Kotlin, and paired with a Reanimated pre-invalidate barrier that upstream lacks: NativeProxy.invalidate() invalidates the shared frame queue before invalidateCpp() releases javaPart_, because TurboModuleManager invalidates Reanimated's module holder first and Worklets therefore cannot stop the queue in time on its own. The queue's dispatch lock is what makes this a barrier rather than a check: invalidation cannot proceed while a frame batch is mid-dispatch, so no AnimationFrameCallback can run after native is cleared. Scope is the observed AnimationFrameCallback path. A controlled real-queue exercise fails before the change and passes after it. A patched Android build and 40 active-animation native reloads are underway with no result yet; nothing is marked fixed or tested until those native outcomes arrive. Separately, the stale installed-host hold is corrected: you explicitly authorized the local update, 0.1.26-dev.local.20260906.62b4e99d is installed, and it delivered the next realtime message successfully. Merge and beta publication stay held, and the remaining beta tasks are deferred until release while the crash fix is the focus.

- 2026-09-06: The patched runtime-reload stress PASSes 40/40 at 11:32:09.686Z with zero failures, crashes or hangs: eight actually advancing Reanimated loops on every reload, a single process (pid 20673) throughout, and a fresh JS runtime each time (runtime-reload-patched.json and runtime-reload-patched-build-provenance.json in the lead watchers; source sha bda55f208173ef540efac149fcdf51f39802ad469385097e861486ee98a81633). The weight of that result is bounded deliberately: the unpatched baseline also passes its 20 reloads, so the native runs do not reproduce the abort on demand and are not a native before-and-after. The only deterministic before-fail and after-pass remains the controlled Java queue flow. Release build V6 is running on a fresh dependency install, with the release logout path after it; final closure stays held and the abort is still not claimed fixed or tested.

- 2026-09-06: The observed AnimationFrameCallback teardown crash is closed within its stated scope on release build V6, source sha bda55f208173ef540efac149fcdf51f39802ad469385097e861486ee98a81633. V6 builds PASS at 11:35:11.735Z; its focused polish flow PASSes at 11:38:34.320Z with no failures on a byte-identical installed APK, sha256 3059c3d5806fd92e78b320e6a6249c402a3427fea1d6c7fe89618753636d76d8 (runtime-reload-release-v6-polish/report.json); and an actual release Settings logout PASSes at 11:39:09.836103Z with the process surviving, the pairing screen returning and the logs proving the real teardown ran — getOrCreateReloadTask, ReactContext and ReactInstance destroyed, surfaces restarting — with zero new fatal signals and only the retained non-fatal soft-teardown warnings (runtime-reload-release-v6-logout.json and .log with screenshots, both lead watcher roots). The claim is bounded exactly as the evidence is: the controlled Java queue flow is the deterministic before-fail and after-pass; the unpatched native baseline also passes its 20 reloads, so the native runs never reproduced the abort on demand; the patched 40/40 bounds behaviour rather than proving a native before-and-after; and the release check is one actual reload, not reload stress. No physical-phone or ARM64 validation has happened and nothing is published. The crash is recorded as fixed and verified within that scope rather than as an accepted beta limitation. V5 evidence stays as historical results on the older, pre-patch source.
