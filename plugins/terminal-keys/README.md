# Terminal keys

Bundled UI-only extension for the phone-friendly terminal key row. It executes no backend code and requests no data or action authority. Disable or unlink `muxr.terminal-keys` to hide the row; terminal rendering and input remain available.
