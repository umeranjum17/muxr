diff --git a/node_modules/bonjour-service/dist/lib/service.d.ts b/node_modules/bonjour-service/dist/lib/service.d.ts
index 9782558..fde4d89 100644
--- a/node_modules/bonjour-service/dist/lib/service.d.ts
+++ b/node_modules/bonjour-service/dist/lib/service.d.ts
@@ -6,6 +6,8 @@ export interface ServiceConfig {
     port: number;
     protocol?: 'tcp' | 'udp';
     host?: string;
+    /** Explicit service addresses; defaults to every non-internal interface. */
+    addresses?: Array<string>;
     fqdn?: string;
     subtypes?: Array<string>;
     txt?: KeyValue;
diff --git a/node_modules/bonjour-service/dist/lib/service.js b/node_modules/bonjour-service/dist/lib/service.js
index 16f6f70..d7210cc 100644
--- a/node_modules/bonjour-service/dist/lib/service.js
+++ b/node_modules/bonjour-service/dist/lib/service.js
@@ -33,6 +33,7 @@ class Service extends events_1.EventEmitter {
         this.fqdn = `${this.name}.${this.type}${TLD}`;
         this.txt = config.txt;
         this.subtypes = config.subtypes;
+        this.addresses = config.addresses;
         this.disableIPv6 = !!config.disableIPv6;
     }
     records() {
@@ -45,6 +46,12 @@ class Service extends events_1.EventEmitter {
         for (let subtype of this.subtypes || []) {
             records.push(this.RecordSubtypePTR(this, subtype));
         }
+        if (this.addresses !== undefined) {
+            for (const address of this.addresses) {
+                records.push(address.includes(':') ? this.RecordAAAA(this, address) : this.RecordA(this, address));
+            }
+            return records;
+        }
         let ifaces = Object.values(os_1.default.networkInterfaces());
         for (let iface of ifaces) {
             let addrs = iface;
