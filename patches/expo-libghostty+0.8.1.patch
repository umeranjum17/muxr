diff --git a/node_modules/expo-libghostty/android/src/main/cpp/ghostty_jni.cpp b/node_modules/expo-libghostty/android/src/main/cpp/ghostty_jni.cpp
index b9c118e..620e92d 100644
--- a/node_modules/expo-libghostty/android/src/main/cpp/ghostty_jni.cpp
+++ b/node_modules/expo-libghostty/android/src/main/cpp/ghostty_jni.cpp
@@ -18,7 +18,8 @@
 //     [10] row record count    [11] cursor blinking (0/1)
 //     [12] cursor color (ARGB) or 0 when unconfigured
 //   row record:
-//     i32 rowIndex, i32 selStartX or -1, i32 selEndX or -1, i32 textUnits
+//     i32 rowIndex, i32 selStartX or -1, i32 selEndX or -1, i32 textUnits,
+//     i32 wrap continuation (0/1: this row continues the row above it)
 //     cells[cols] x 16 bytes:
 //       i32 fg (ARGB), i32 bg (ARGB),
 //       u16 textOffset (UTF-16 units into row blob), u16 textLen (units),
@@ -32,6 +33,7 @@
 
 #include <cstdint>
 #include <cstring>
+#include <string>
 #include <vector>
 
 #include <ghostty/vt.h>
@@ -52,9 +54,12 @@ constexpr uint16_t kFlagWide = 1 << 7;
 constexpr uint16_t kFlagSpacer = 1 << 8;
 constexpr uint16_t kFlagFgDefault = 1 << 9;
 constexpr uint16_t kFlagBgNone = 1 << 10;
+// Cell carries an OSC 8 hyperlink; renderers underline it and resolve the
+// URI lazily via nativeHyperlinkUri (one grid-ref lookup per interaction).
+constexpr uint16_t kFlagHyperlink = 1 << 11;
 
 constexpr size_t kHeaderBytes = 13 * sizeof(int32_t);
-constexpr size_t kRowHeaderBytes = 4 * sizeof(int32_t);
+constexpr size_t kRowHeaderBytes = 5 * sizeof(int32_t);
 constexpr size_t kCellRecordBytes = 16;
 // Cap per-cell grapheme text; anything longer is truncated (degenerate input).
 constexpr uint32_t kMaxCellTextUnits = 32;
@@ -507,6 +512,14 @@ Java_expo_modules_libghostty_GhosttyVt_nativeSnapshot(
     rowHeader[1] = selResult == GHOSTTY_SUCCESS ? selection.start_x : -1;
     rowHeader[2] = selResult == GHOSTTY_SUCCESS ? selection.end_x : -1;
 
+    GhosttyRow rawRow = 0;
+    bool wrapContinuation = false;
+    if (ghostty_render_state_row_get(session->rowIter, GHOSTTY_RENDER_STATE_ROW_DATA_RAW,
+                                     &rawRow) == GHOSTTY_SUCCESS) {
+      ghostty_row_get(rawRow, GHOSTTY_ROW_DATA_WRAP_CONTINUATION, &wrapContinuation);
+    }
+    rowHeader[4] = wrapContinuation ? 1 : 0;
+
     if (ghostty_render_state_row_get(session->rowIter, GHOSTTY_RENDER_STATE_ROW_DATA_CELLS,
                                      &session->cells) != GHOSTTY_SUCCESS) {
       return -1;
@@ -534,6 +547,10 @@ Java_expo_modules_libghostty_GhosttyVt_nativeSnapshot(
         flags |= kFlagSpacer;
       }
 
+      bool hasHyperlink = false;
+      ghostty_cell_get(raw, GHOSTTY_CELL_DATA_HAS_HYPERLINK, &hasHyperlink);
+      if (hasHyperlink) flags |= kFlagHyperlink;
+
       bool hasStyling = false;
       ghostty_render_state_row_cells_get(session->cells,
                                          GHOSTTY_RENDER_STATE_ROW_CELLS_DATA_HAS_STYLING,
@@ -628,6 +645,86 @@ Java_expo_modules_libghostty_GhosttyVt_nativeSnapshot(
   return rowCount;
 }
 
+// Returns the OSC 8 hyperlink URI at a viewport cell, or null when the cell
+// has no hyperlink. One grid-ref snapshot per call; interaction-time only.
+JNIEXPORT jstring JNICALL
+Java_expo_modules_libghostty_GhosttyVt_nativeHyperlinkUri(
+    JNIEnv* env, jobject, jlong handle, jint col, jint row) {
+  auto* session = fromHandle(handle);
+  if (session == nullptr || session->term == nullptr) return nullptr;
+  GhosttyGridRef ref{};
+  ref.size = sizeof(ref);
+  if (!viewportGridRef(session, col, row, &ref)) return nullptr;
+  size_t needed = 0;
+  if (ghostty_grid_ref_hyperlink_uri(&ref, nullptr, 0, &needed) != GHOSTTY_OUT_OF_SPACE ||
+      needed == 0) {
+    return nullptr;
+  }
+  // ghostty_grid_ref_hyperlink_uri writes exactly `needed` bytes without a
+  // terminator; keep one spare NUL byte so the decode below can strlen-read.
+  std::vector<uint8_t> uri(needed + 1, 0);
+  size_t written = 0;
+  if (ghostty_grid_ref_hyperlink_uri(&ref, uri.data(), uri.size(), &written) != GHOSTTY_SUCCESS ||
+      written == 0) {
+    return nullptr;
+  }
+  // NewStringUTF requires modified UTF-8, not standard UTF-8: astral
+  // codepoints must arrive as surrogate pairs and embedded NULs as 0xC0 0x80.
+  // 2- and 3-byte sequences are identical in both encodings. Anything that
+  // is not valid UTF-8 fails closed to null.
+  std::string modified;
+  modified.reserve(written * 3 / 2 + 1);
+  for (size_t i = 0; i < written;) {
+    const uint8_t lead = uri[i];
+    if (lead < 0x80) {
+      if (lead == 0) {
+        modified.push_back(static_cast<char>(0xC0));
+        modified.push_back(static_cast<char>(0x80));
+      } else {
+        modified.push_back(static_cast<char>(lead));
+      }
+      i++;
+      continue;
+    }
+    uint32_t cp = 0;
+    size_t len = 0;
+    if ((lead & 0xE0) == 0xC0 && i + 1 < written) {
+      cp = lead & 0x1F;
+      len = 2;
+    } else if ((lead & 0xF0) == 0xE0 && i + 2 < written) {
+      cp = lead & 0x0F;
+      len = 3;
+    } else if ((lead & 0xF8) == 0xF0 && i + 3 < written) {
+      cp = lead & 0x07;
+      len = 4;
+    } else {
+      return nullptr;
+    }
+    for (size_t k = 1; k < len; k++) {
+      if ((uri[i + k] & 0xC0) != 0x80) return nullptr;
+      cp = (cp << 6) | (uri[i + k] & 0x3F);
+    }
+    if ((len == 2 && cp < 0x80) || (len == 3 && cp < 0x800) ||
+        (len == 4 && cp < 0x10000) || (cp >= 0xD800 && cp <= 0xDFFF)) {
+      return nullptr;
+    }
+    if (len == 4) {
+      if (cp > 0x10FFFF) return nullptr;
+      cp -= 0x10000;
+      const uint32_t surrogates[2] = {0xD800 + (cp >> 10), 0xDC00 + (cp & 0x3FF)};
+      for (const uint32_t s : surrogates) {
+        modified.push_back(static_cast<char>(0xE0 | (s >> 12)));
+        modified.push_back(static_cast<char>(0x80 | ((s >> 6) & 0x3F)));
+        modified.push_back(static_cast<char>(0x80 | (s & 0x3F)));
+      }
+    } else {
+      modified.append(reinterpret_cast<const char*>(uri.data() + i), len);
+    }
+    i += len;
+  }
+  return env->NewStringUTF(modified.c_str());
+}
+
 JNIEXPORT jboolean JNICALL
 Java_expo_modules_libghostty_GhosttyVt_nativeSelectWord(
     JNIEnv*, jobject, jlong handle, jint col, jint row) {
diff --git a/node_modules/expo-libghostty/android/src/main/java/expo/modules/libghostty/ExpoLibghosttyModule.kt b/node_modules/expo-libghostty/android/src/main/java/expo/modules/libghostty/ExpoLibghosttyModule.kt
index 1516400..42997c3 100644
--- a/node_modules/expo-libghostty/android/src/main/java/expo/modules/libghostty/ExpoLibghosttyModule.kt
+++ b/node_modules/expo-libghostty/android/src/main/java/expo/modules/libghostty/ExpoLibghosttyModule.kt
@@ -12,13 +12,25 @@ class ExpoLibghosttyModule : Module() {
     Name("ExpoLibghostty")
 
     View(ExpoLibghosttyView::class) {
-      Events("onInput", "onResize")
+      Events("onInput", "onResize", "onScroll", "onOpenLink", "onPinch")
 
       // Base font size in dp (default 14); applied live, the grid reflows.
       Prop("fontSize") { view: ExpoLibghosttyView, size: Float ->
         view.setFontSize(size)
       }
 
+      Prop("autoShowKeyboard") { view: ExpoLibghosttyView, enabled: Boolean ->
+        view.setAutoShowKeyboard(enabled)
+      }
+
+      AsyncFunction("showKeyboard") { view: ExpoLibghosttyView ->
+        view.showKeyboard()
+      }
+
+      AsyncFunction("hideKeyboard") { view: ExpoLibghosttyView ->
+        view.hideKeyboard()
+      }
+
       // Theme colors (ghostty config syntax); null clears to the defaults.
       Prop("theme") { view: ExpoLibghosttyView, theme: TerminalThemeRecord? ->
         view.setTheme(theme)
diff --git a/node_modules/expo-libghostty/android/src/main/java/expo/modules/libghostty/ExpoLibghosttyView.kt b/node_modules/expo-libghostty/android/src/main/java/expo/modules/libghostty/ExpoLibghosttyView.kt
index 2d3ed62..aa9d6a9 100644
--- a/node_modules/expo-libghostty/android/src/main/java/expo/modules/libghostty/ExpoLibghosttyView.kt
+++ b/node_modules/expo-libghostty/android/src/main/java/expo/modules/libghostty/ExpoLibghosttyView.kt
@@ -19,6 +19,9 @@ class ExpoLibghosttyView(context: Context, appContext: AppContext) :
 
   private val onInput by EventDispatcher<Map<String, Any>>()
   private val onResize by EventDispatcher<Map<String, Any>>()
+  private val onScroll by EventDispatcher<Map<String, Any>>()
+  private val onOpenLink by EventDispatcher<Map<String, Any>>()
+  private val onPinch by EventDispatcher<Map<String, Any>>()
 
   private val terminal = GhosttyTerminalView(context).also { view ->
     view.onInputBytes = { bytes ->
@@ -32,6 +35,15 @@ class ExpoLibghosttyView(context: Context, appContext: AppContext) :
     view.onGridResize = { cols, rows ->
       onResize(mapOf("cols" to cols, "rows" to rows))
     }
+    view.onScrollRows = { rows ->
+      onScroll(mapOf("rows" to rows))
+    }
+    view.onOpenLink = { url ->
+      onOpenLink(mapOf("url" to url))
+    }
+    view.onPinch = { phase, scale ->
+      onPinch(mapOf("phase" to phase, "scale" to scale.toDouble()))
+    }
   }
 
   private val accessoryBar = TerminalAccessoryBar(context).also { bar ->
@@ -56,21 +68,12 @@ class ExpoLibghosttyView(context: Context, appContext: AppContext) :
   }
 
   private fun syncAccessoryBar(insets: WindowInsetsCompat) {
-    val imeVisible = insets.isVisible(WindowInsetsCompat.Type.ime())
-    accessoryBar.visibility = if (imeVisible) VISIBLE else GONE
-    // Edge-to-edge windows don't resize for the keyboard — it just covers us.
-    // Pad our bottom by the covered amount so the bar sits above the IME and
-    // the terminal shrinks (regaining the hidden rows). Hosts that already
-    // handle the keyboard leave no overlap, so the padding stays zero.
-    val imeBottom = insets.getInsets(WindowInsetsCompat.Type.ime()).bottom
-    val location = IntArray(2)
-    getLocationInWindow(location)
-    val gapBelow = rootView.height - (location[1] + height)
-    val overlap = if (imeVisible) (imeBottom - gapBelow).coerceAtLeast(0) else 0
-    if (paddingBottom != overlap) {
-      setPadding(0, 0, 0, overlap)
-      requestLayout()
-    }
+    // muxr ships its own key toolbar; two stacked bars is the bug.
+    // JS (TerminalScreen) is the sole keyboard-avoidance owner: native
+    // IME-overlap padding computed against pre-JS geometry on the first
+    // insets event double-shrinks the terminal, then a later event recomputes
+    // overlap to zero and it snaps back up.
+    accessoryBar.visibility = GONE
   }
 
   override fun onSizeChanged(w: Int, h: Int, oldw: Int, oldh: Int) {
@@ -82,6 +85,10 @@ class ExpoLibghosttyView(context: Context, appContext: AppContext) :
   /** Base font size in dp; applied live (the grid reflows in place). */
   fun setFontSize(dp: Float) = terminal.setFontSize(dp)
 
+  fun setAutoShowKeyboard(enabled: Boolean) { terminal.autoShowKeyboard = enabled }
+  fun showKeyboard() = terminal.showKeyboard()
+  fun hideKeyboard() = terminal.hideKeyboard()
+
   /** Apply theme colors; a null record clears back to the defaults. */
   internal fun setTheme(theme: TerminalThemeRecord?) = terminal.setTheme(
     foreground = theme?.foreground,
diff --git a/node_modules/expo-libghostty/android/src/main/java/expo/modules/libghostty/GhosttyTerminalView.kt b/node_modules/expo-libghostty/android/src/main/java/expo/modules/libghostty/GhosttyTerminalView.kt
index 9d6ad16..0bd5332 100644
--- a/node_modules/expo-libghostty/android/src/main/java/expo/modules/libghostty/GhosttyTerminalView.kt
+++ b/node_modules/expo-libghostty/android/src/main/java/expo/modules/libghostty/GhosttyTerminalView.kt
@@ -9,7 +9,11 @@ import android.graphics.Canvas
 import android.graphics.Paint
 import android.graphics.Path
 import android.graphics.Rect
+import android.graphics.RenderNode
 import android.graphics.Typeface
+import android.os.Build
+import android.os.SystemClock
+import androidx.annotation.RequiresApi
 import android.provider.Settings
 import android.text.InputType
 import android.view.ActionMode
@@ -30,6 +34,7 @@ import android.view.inputmethod.InputMethodManager
 import android.widget.OverScroller
 import java.nio.ByteBuffer
 import java.nio.ByteOrder
+import kotlin.math.abs
 import kotlin.math.ceil
 import kotlin.math.floor
 import kotlin.math.hypot
@@ -42,26 +47,64 @@ import kotlin.math.roundToInt
  * Owns a libghostty-vt session via [GhosttyVt]. PTY output is fed in with
  * [write]; user input (IME text, key escape sequences, VT query responses)
  * comes back through [onInputBytes]. Rendering pulls flattened dirty-row
- * snapshots from JNI once per Choreographer frame and patches them onto a
- * grid-sized bitmap; [onDraw] blits the bitmap and overlays the cursor.
+ * snapshots from JNI once per Choreographer frame. API 29+ records dirty
+ * rows; older releases patch a bitmap. [onDraw] paints the cursor on top.
  */
 internal class GhosttyTerminalView(context: Context) : View(context) {
   var onInputBytes: ((ByteArray) -> Unit)? = null
   var onGridResize: ((cols: Int, rows: Int) -> Unit)? = null
+  var autoShowKeyboard = true
+  /** Set when scrollback lives on a server; suppresses the local buffer,
+   *  which holds only repaint diffs and shows garbage when scrolled. */
+  var onScrollRows: ((rows: Int) -> Unit)? = null
 
   /** Sticky-modifier source for accessory-bar keys and IME text (optional). */
   var accessoryBar: TerminalAccessoryBar? = null
 
+  /** A link the user tapped in the grid. The string is exact output text or
+   *  an OSC 8 URI; opening is validated and performed by the JS layer. */
+  var onOpenLink: ((url: String) -> Unit)? = null
+
+  /** Set when the host owns text size: a pinch is reported ("begin",
+   *  "change" with the scale since it began, "end") and the grid leaves its
+   *  own size alone, so the host can step a ladder, keep the result and
+   *  hand it back through the fontSize prop -- or ignore the pinch. */
+  var onPinch: ((phase: String, scale: Float) -> Unit)? = null
+
   private var handle: Long = GhosttyVt.nativeCreate(INITIAL_COLS, INITIAL_ROWS, MAX_SCROLLBACK)
   private var finished = false
 
   // ── Cell metrics ──
   private var fontSizeDp = FONT_SIZE_DP
-  private val textPaint = newTextPaint(Typeface.MONOSPACE)
-  private val boldPaint = newTextPaint(Typeface.create(Typeface.MONOSPACE, Typeface.BOLD))
-  private val italicPaint = newTextPaint(Typeface.create(Typeface.MONOSPACE, Typeface.ITALIC))
+  // The grid is cellWidth = advance of "M", and every glyph is drawn at
+  // col * cellWidth, so the face must be genuinely monospaced. The system's
+  // Typeface.MONOSPACE is not a dependable contract: a ROM font
+  // customization, an accessibility font or an OEM variable-font
+  // substitution can resolve it to a proportional face, and then every
+  // glyph sits in an "M"-wide cell, letter-spaced, wrapping at ~30 columns.
+  // So the text is set in a face the app bundles, the way PUA cells already
+  // use the bundled symbols font, and the system face is only the fallback.
+  private val bundledMono: Typeface? = try {
+    Typeface.createFromAsset(context.assets, TEXT_FONT_ASSET)
+  } catch (e: RuntimeException) {
+    null
+  }
+  private val bundledMonoItalic: Typeface? = try {
+    Typeface.createFromAsset(context.assets, TEXT_FONT_ITALIC_ASSET)
+  } catch (e: RuntimeException) {
+    null
+  }
+  private val bundledMonoBold: Typeface? = try {
+    Typeface.createFromAsset(context.assets, TEXT_FONT_BOLD_ASSET)
+  } catch (e: RuntimeException) {
+    null
+  }
+  private val monoTypeface: Typeface = bundledMono ?: Typeface.MONOSPACE
+  private val textPaint = newTextPaint(monoTypeface)
+  private val boldPaint = newTextPaint(bundledMonoBold ?: Typeface.create(monoTypeface, Typeface.BOLD))
+  private val italicPaint = newTextPaint(bundledMonoItalic ?: Typeface.create(monoTypeface, Typeface.ITALIC))
   private val boldItalicPaint =
-    newTextPaint(Typeface.create(Typeface.MONOSPACE, Typeface.BOLD_ITALIC))
+    newTextPaint(Typeface.create(bundledMonoBold ?: monoTypeface, Typeface.BOLD_ITALIC))
 
   // Nerd Font private-use glyphs (powerline, devicons, …) are absent from the
   // system fallback chain; route those cells to the bundled symbols-only font.
@@ -71,8 +114,8 @@ internal class GhosttyTerminalView(context: Context) : View(context) {
     null
   }
   private val fillPaint = Paint()
   private val cursorPaint = Paint()
-  private var cellWidth = textPaint.measureText("M")
+  private var cellWidth = measureCellWidth()
   private var cellHeight = ceil(
     textPaint.fontMetrics.descent - textPaint.fontMetrics.ascent
   ).toInt()
@@ -82,6 +125,13 @@ internal class GhosttyTerminalView(context: Context) : View(context) {
   // ── Grid state ──
   private var cols = INITIAL_COLS
   private var rows = INITIAL_ROWS
+  // One display list per grid row (API 29+): a dirty row is re-recorded and
+  // the rest replay untouched, and the GPU draws the glyphs. Patching rows
+  // onto one grid-sized bitmap instead rasterised text on the UI thread and
+  // re-uploaded the whole bitmap to the GPU on every frame that changed a
+  // single cell, which is what made a streaming terminal miss its frames.
+  private val rowDisplayLists: RowRenderer? = if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.Q) RowDisplayLists(this) else null
+  // Older releases keep the bitmap.
   private var bitmap: Bitmap? = null
   private var bitmapCanvas: Canvas? = null
   private var snapshotBuf: ByteBuffer? = null
@@ -95,6 +145,15 @@ internal class GhosttyTerminalView(context: Context) : View(context) {
   private var rowSelEnd = IntArray(0)
   private var rowSelEndWide = BooleanArray(0)
 
+  /** Link ranges per viewport row, rebuilt each time the row is redrawn.
+   *  [LinkRange.segment] is empty for OSC 8 cells; their URI resolves at
+   *  hit-test time through [GhosttyVt.nativeHyperlinkUri]. */
+  private var rowLinks = arrayOfNulls<Array<LinkRange>>(0)
+  private var rowLinkMask = arrayOfNulls<BooleanArray>(0)
+  /** Row continues the row above it (vt WRAP_CONTINUATION); a hard new line
+   *  is false, so a full-width URL above must never join into it. */
+  private var rowWrapCont = BooleanArray(0)
+
   // ── Selection state ──
   private var selectionMode = false
   private var actionMode: ActionMode? = null
@@ -126,15 +185,24 @@ internal class GhosttyTerminalView(context: Context) : View(context) {
   private var cursorVisible = false
   private var cursorBlinks = false
 
   // The cursor is an onDraw overlay, so blinking only re-blits the cached
   // bitmap; the grid is untouched.
   private var blinkPhaseOn = true
   private var blinkScheduled = false
+  /** Last input/output; the blink stops (cursor stays solid) after a quiet period. */
+  private var lastTerminalActivityAt = SystemClock.uptimeMillis()
   private val blinkRunnable = object : Runnable {
     override fun run() {
       if (!blinkScheduled) return
+      if (SystemClock.uptimeMillis() - lastTerminalActivityAt >= BLINK_IDLE_STOP_MS) {
+        // Quiet: hold the cursor solid and stop waking the GPU every 600 ms.
+        blinkScheduled = false
+        blinkPhaseOn = true
+        invalidate()
+        return
+      }
       blinkPhaseOn = !blinkPhaseOn
       invalidate()
       postDelayed(this, BLINK_INTERVAL_MS)
     }
   }
@@ -166,7 +234,7 @@ internal class GhosttyTerminalView(context: Context) : View(context) {
       val deltaRows = (scrollAccum / cellHeight).toInt()
       if (deltaRows != 0) {
         scrollAccum -= deltaRows * cellHeight
-        GhosttyVt.nativeScroll(handle, deltaRows)
+        onScrollRows?.let { it(deltaRows) } ?: GhosttyVt.nativeScroll(handle, deltaRows)
         markScrollActivity()
         scheduleFrame()
       }
@@ -193,9 +261,14 @@ internal class GhosttyTerminalView(context: Context) : View(context) {
           clearSelection()
           return true
         }
+        // A tap on a link opens it; it must not raise the keyboard or focus
+        // the terminal, which would steal the window from the browser.
+        linkUriAt((e.x / cellWidth).toInt(), (e.y / cellHeight).toInt())?.let { url ->
+          onOpenLink?.invoke(url)
+          return true
+        }
         requestFocus()
-        context.getSystemService(InputMethodManager::class.java)
-          ?.showSoftInput(this@GhosttyTerminalView, 0)
+        if (autoShowKeyboard) showKeyboard()
         return true
       }
 
@@ -204,6 +277,12 @@ internal class GhosttyTerminalView(context: Context) : View(context) {
         lastPressCol = (e.x / cellWidth).toInt().coerceIn(0, cols - 1)
         lastPressRow = (e.y / cellHeight).toInt().coerceIn(0, rows - 1)
         performHapticFeedback(HapticFeedbackConstants.LONG_PRESS)
+        // Long-press on a link copies the exact link instead of selecting the
+        // word under the finger.
+        linkUriAt(lastPressCol, lastPressRow)?.let { url ->
+          copyLink(url)
+          return
+        }
         selectionMode = GhosttyVt.nativeSelectWord(handle, lastPressCol, lastPressRow)
         scheduleFrame()
         if (selectionMode) {
@@ -229,7 +308,7 @@ internal class GhosttyTerminalView(context: Context) : View(context) {
         val deltaRows = (scrollAccum / cellHeight).toInt()
         if (deltaRows != 0) {
           scrollAccum -= deltaRows * cellHeight
-          GhosttyVt.nativeScroll(handle, deltaRows)
+          onScrollRows?.let { it(deltaRows) } ?: GhosttyVt.nativeScroll(handle, deltaRows)
           markScrollActivity()
           scheduleFrame()
         }
@@ -262,11 +341,19 @@ internal class GhosttyTerminalView(context: Context) : View(context) {
       override fun onScaleBegin(detector: ScaleGestureDetector): Boolean {
         pinchScale = 1f
         pinchLastScale = 1f
+        onPinch?.invoke("begin", 1f)
         return true
       }
 
       override fun onScale(detector: ScaleGestureDetector): Boolean {
         pinchScale *= detector.scaleFactor
+        onPinch?.let { report ->
+          if (abs(pinchScale - pinchLastScale) >= PINCH_REPORT_STEP) {
+            pinchLastScale = pinchScale
+            report("change", pinchScale)
+          }
+          return true
+        }
         val steps = ((pinchScale - pinchLastScale) / PINCH_SCALE_STEP).toInt()
         if (steps != 0) {
           pinchLastScale += steps * PINCH_SCALE_STEP
@@ -274,6 +361,10 @@ internal class GhosttyTerminalView(context: Context) : View(context) {
         }
         return true
       }
+
+      override fun onScaleEnd(detector: ScaleGestureDetector) {
+        onPinch?.invoke("end", pinchScale)
+      }
     }
   )
 
@@ -285,6 +376,28 @@ internal class GhosttyTerminalView(context: Context) : View(context) {
   private fun newTextPaint(typeface: Typeface) = Paint(Paint.ANTI_ALIAS_FLAG).apply {
     this.typeface = typeface
     textSize = fontSizeDp * resources.displayMetrics.density
+    // Cells drawn as one run must render exactly the glyphs they would
+    // alone: no ligatures, contextual alternates or kerning.
+    fontFeatureSettings = "'liga' 0, 'calt' 0, 'kern' 0"
+  }
+
+  /**
+   * The cell is the advance of "M" in the face the text is drawn with. If
+   * that face turns out not to be monospaced -- the bundled asset failed to
+   * load and the system face is proportional -- the grid and the glyphs
+   * disagree and the output reads letter-spaced; say so once in the log
+   * rather than silently, since there is no narrower cell that would fit.
+   */
+  private fun measureCellWidth(): Float {
+    val wide = textPaint.measureText("M")
+    val narrow = textPaint.measureText("i")
+    if (kotlin.math.abs(wide - narrow) > 0.5f) {
+      android.util.Log.w(
+        "GhosttyTerminalView",
+        "terminal face is not monospaced (M=$wide i=$narrow); bundled=${bundledMono != null}"
+      )
+    }
+    return wide
   }
 
   /**
@@ -300,7 +413,7 @@ internal class GhosttyTerminalView(context: Context) : View(context) {
     for (paint in arrayOf(textPaint, boldPaint, italicPaint, boldItalicPaint, symbolsPaint)) {
       paint?.textSize = px
     }
-    cellWidth = textPaint.measureText("M")
+    cellWidth = measureCellWidth()
     cellHeight = ceil(textPaint.fontMetrics.descent - textPaint.fontMetrics.ascent).toInt()
     baseline = -textPaint.fontMetrics.ascent
     // Cell geometry moved under any active selection handles.
@@ -342,6 +455,7 @@ internal class GhosttyTerminalView(context: Context) : View(context) {
     if (response != null && response.isNotEmpty() && !finished) {
       onInputBytes?.invoke(response)
     }
+    lastTerminalActivityAt = SystemClock.uptimeMillis()
     holdBlinkSolid()
     scheduleFrame()
   }
@@ -365,9 +479,11 @@ internal class GhosttyTerminalView(context: Context) : View(context) {
       GhosttyVt.nativeFree(handle)
       handle = 0
     }
+    rowDisplayLists?.release()
   }
 
   private fun sendBytes(bytes: ByteArray) {
+    lastTerminalActivityAt = SystemClock.uptimeMillis()
     if (!finished && bytes.isNotEmpty()) {
       if (selectionMode) clearSelection()
       // Typing while scrolled into history jumps back to the live view.
@@ -454,7 +570,7 @@ internal class GhosttyTerminalView(context: Context) : View(context) {
     val changed = newCols != cols || newRows != rows
     cols = newCols
     rows = newRows
-    if (changed || force || bitmap == null) {
+    if (changed || force || snapshotBuf == null) {
       GhosttyVt.nativeResize(handle, cols, rows, cellWidth.roundToInt(), cellHeight)
       allocateGridBuffers()
       onGridResize?.invoke(cols, rows)
@@ -465,9 +581,27 @@ internal class GhosttyTerminalView(context: Context) : View(context) {
   private fun allocateGridBuffers() {
     val widthPx = max(1, ceil(cols * cellWidth).toInt())
     val heightPx = max(1, rows * cellHeight)
-    bitmap = Bitmap.createBitmap(widthPx, heightPx, Bitmap.Config.ARGB_8888).also {
-      bitmapCanvas = Canvas(it)
-      bitmapCanvas?.drawColor(defaultBg)
+    // On older releases, rebuilding the bitmap used to publish a blank
+    // frame: the fresh bitmap replaced the presented one during layout, but
+    // the repaint that refills it only lands on the next Choreographer frame
+    // — and animated layouts cross many intermediate sizes. Seed the new
+    // surface with the previous frame so the published transition frame is
+    // continuous; subsequent snapshots repaint rows onto it in place. The
+    // surface must never be gated on the render state's global dirty class:
+    // that flag is caller-managed (see ghostty/vt/render.h) and a partial
+    // classification must never leave a new surface unpublished.
+    if (rowDisplayLists != null) {
+      // Keep the previous row recordings through a resize until repaint.
+      rowDisplayLists.resize(rows)
+    } else {
+      val previous = bitmap
+      bitmap = Bitmap.createBitmap(widthPx, heightPx, Bitmap.Config.ARGB_8888).also {
+        bitmapCanvas = Canvas(it)
+        bitmapCanvas?.drawColor(defaultBg)
+        if (previous != null && !previous.isRecycled) {
+          bitmapCanvas?.drawBitmap(previous, 0f, 0f, null)
+        }
+      }
     }
     val capacity = HEADER_BYTES +
       rows * (ROW_HEADER_BYTES + cols * CELL_RECORD_BYTES + cols * MAX_CELL_TEXT_UNITS * 2 + 4)
@@ -481,6 +615,9 @@ internal class GhosttyTerminalView(context: Context) : View(context) {
     rowSelStart = IntArray(rows) { -1 }
     rowSelEnd = IntArray(rows) { -1 }
     rowSelEndWide = BooleanArray(rows)
+    rowLinks = arrayOfNulls(rows)
+    rowLinkMask = arrayOfNulls(rows)
+    rowWrapCont = BooleanArray(rows)
   }
 
   // ── Rendering ──
@@ -493,7 +630,8 @@ internal class GhosttyTerminalView(context: Context) : View(context) {
 
   private fun renderFrame() {
     val buf = snapshotBuf ?: return
-    val canvas = bitmapCanvas ?: return
+    val canvas = bitmapCanvas
+    if (rowDisplayLists == null && canvas == null) return
     if (handle == 0L) return
 
     val rowCount = GhosttyVt.nativeSnapshot(handle, buf)
@@ -519,13 +657,164 @@ internal class GhosttyTerminalView(context: Context) : View(context) {
     cursorBlinks = buf.int != 0
     cursorColor = buf.int
     syncBlinkTimer()
 
-    if (dirtyKind == DIRTY_FULL) canvas.drawColor(defaultBg)
-    repeat(rowCount) { drawRowRecord(buf, canvas, snapCols) }
+    if (canvas == null) {
+      // A full snapshot redraws the grid: a row it leaves out is background.
+      if (dirtyKind == DIRTY_FULL) rowDisplayLists?.clear()
+      repeat(rowCount) { rowDisplayLists?.recordRow(buf, snapCols) }
+    } else {
+      if (dirtyKind == DIRTY_FULL) canvas.drawColor(defaultBg)
+      repeat(rowCount) { drawRowRecord(buf, canvas, snapCols) }
+    }
     syncSelectionUi()
     invalidate()
   }
 
+  // ── Links ──
+
+  /** One contiguous link run in a row. [segment] is the exact on-screen text;
+   *  empty for OSC 8 runs, whose URI resolves at hit-test time. */
+  private class LinkRange(val startCol: Int, val endCol: Int, val segment: String, val fromWrap: Boolean = false) {
+    val isOsc8: Boolean get() = segment.isEmpty()
+  }
+
+  /** Rebuild the link ranges for a redrawn row: OSC 8 runs from cell flags,
+   *  plain http(s) URLs from the row text, and one continuation run when the
+   *  previous row's URL reached the final column (a wrapped URL). */
+  private fun updateRowLinks(rowIndex: Int, textUnits: Int, wrapContinuation: Boolean) {
+    if (rowIndex >= rowLinks.size) return
+    // A redraw here invalidates wrap-continuation ranges the row below
+    // cached against this row's previous content.
+    val below = rowLinks.getOrNull(rowIndex + 1)
+    if (below != null && below.any { it.fromWrap }) {
+      val kept = below.filter { !it.fromWrap }
+      rowLinks[rowIndex + 1] = if (kept.isEmpty()) null else kept.toTypedArray()
+    }
+    val mask = BooleanArray(cols)
+    val ranges = ArrayList<LinkRange>(2)
+
+    var col = 0
+    while (col < cols) {
+      if (cellFlags[col] and FLAG_HYPERLINK == 0) { col++; continue }
+      val start = col
+      while (col < cols && cellFlags[col] and FLAG_HYPERLINK != 0) col++
+      ranges.add(LinkRange(start, col - 1, ""))
+      for (c in start until col) mask[c] = true
+    }
+
+    if (textUnits > 0) {
+      val colOf = IntArray(textUnits) { -1 }
+      for (c in 0 until minOf(cols, cellTextLen.size)) {
+        for (i in 0 until cellTextLen[c]) {
+          val idx = cellTextOff[c] + i
+          if (idx < textUnits) colOf[idx] = c
+        }
+      }
+
+      var i = 0
+      while (i < textUnits) {
+        val authorityStart = urlAuthorityStartAt(i, textUnits)
+        if (authorityStart < 0) { i++; continue }
+        var end = authorityStart
+        while (end < textUnits && !isUrlBreak(rowChars[end])) end++
+        val startCol = colOf[i]
+        val endCol = colOf[end - 1]
+        if (startCol >= 0 && endCol >= startCol) {
+          ranges.add(LinkRange(startCol, endCol, String(rowChars, i, end - i)))
+          for (c in startCol..endCol) mask[c] = true
+        }
+        i = end.coerceAtLeast(i + 1)
+      }
+
+      // A wrapped URL continues onto this row only when the vt continuation
+      // flag says this row is soft-wrapped from the one above and the row
+      // above ended with a plain URL run at the final column. A hard new
+      // line starts fresh, even below a full-width URL.
+      if (rowIndex > 0 && wrapContinuation && ranges.none { it.startCol == 0 && !it.isOsc8 }) {
+        val parent = rowLinks.getOrNull(rowIndex - 1)
+          ?.lastOrNull { !it.isOsc8 && it.endCol >= cols - 1 }
+        if (parent != null) {
+          var end = 0
+          while (end < textUnits && !isUrlBreak(rowChars[end])) end++
+          val endCol = if (end > 0) colOf[end - 1] else -1
+          if (endCol >= 0) {
+            ranges.add(LinkRange(0, endCol, String(rowChars, 0, end), fromWrap = true))
+            for (c in 0..endCol) mask[c] = true
+          }
+        }
+      }
+    }
+
+    ranges.sortBy { it.startCol }
+    rowLinks[rowIndex] = if (ranges.isEmpty()) null else ranges.toTypedArray()
+    rowLinkMask[rowIndex] = mask
+  }
+
+  /** Index just past the "scheme://" of an http(s) URL at [i], or -1. */
+  private fun urlAuthorityStartAt(i: Int, textUnits: Int): Int {
+    if (i + 7 > textUnits) return -1
+    if (rowChars[i] != 'h' && rowChars[i] != 'H') return -1
+    val slice = String(rowChars, i, minOf(8, textUnits - i))
+    val offset = when {
+      slice.startsWith("http://") || slice.startsWith("HTTP://") -> 7
+      slice.startsWith("https://") || slice.startsWith("HTTPS://") -> 8
+      else -> -1
+    }
+    return if (offset < 0) -1 else i + offset
+  }
+
+  private fun isUrlBreak(c: Char): Boolean =
+    c.isWhitespace() || c == '<' || c == '>' || c == '"' || c == '\'' || c == '`' || c == '|' ||
+      c == '!' || c == '*' || c == '(' || c == ')' || c == '{' || c == '}' || c == '\\' || c == '^'
+
+  /**
+   * The exact link string at a viewport cell: the OSC 8 URI when the cell is
+   * part of a hyperlink run, otherwise the plain URL joined across wrapped
+   * rows. Returns the printed text as-is apart from trailing URL delimiters.
+   */
+  private fun linkUriAt(col: Int, row: Int): String? {
+    if (handle == 0L || finished) return null
+    val c = col.coerceIn(0, cols - 1)
+    val r = row.coerceIn(0, rows - 1)
+    GhosttyVt.nativeHyperlinkUri(handle, c, r)?.takeIf { it.isNotEmpty() }?.let { return it }
+    val tapped = rowLinks.getOrNull(r)
+      ?.firstOrNull { !it.isOsc8 && c in it.startCol..it.endCol } ?: return null
+    var text = tapped.segment
+    var topRow = r
+    var topRange = tapped
+    while (topRow > 0 && rowWrapCont.getOrNull(topRow) == true && topRange.startCol == 0 && text.length < LINK_MAX_CHARS) {
+      val prev = rowLinks.getOrNull(topRow - 1)
+        ?.lastOrNull { !it.isOsc8 && it.endCol >= cols - 1 } ?: break
+      text = prev.segment + text
+      topRow--
+      topRange = prev
+    }
+    // A continuation range at the top of the chain has no validating parent above.
+    if (topRange.fromWrap) return null
+    var bottomRow = r
+    var bottomRange = tapped
+    while (bottomRange.endCol >= cols - 1 && rowWrapCont.getOrNull(bottomRow + 1) == true && text.length < LINK_MAX_CHARS) {
+      val next = rowLinks.getOrNull(bottomRow + 1)
+        ?.firstOrNull { !it.isOsc8 && it.startCol == 0 && !looksLikeFreshUrl(it.segment) } ?: break
+      text += next.segment
+      bottomRow++
+      bottomRange = next
+    }
+    while (text.isNotEmpty() && text.last() in URL_TRAILING_TRIM) text = text.dropLast(1)
+    return text.takeIf { it.isNotEmpty() }
+  }
+
+  private fun looksLikeFreshUrl(segment: String): Boolean {
+    val lower = segment.lowercase()
+    return lower.startsWith("http://") || lower.startsWith("https://")
+  }
+
+  private fun copyLink(url: String) {
+    val clipboard = context.getSystemService(ClipboardManager::class.java) ?: return
+    clipboard.setPrimaryClip(ClipData.newPlainText("terminal link", url))
+    android.widget.Toast.makeText(context, "Link copied", android.widget.Toast.LENGTH_SHORT).show()
+  }
+
   // ── Selection ──
 
   /** Top-left / bottom-right cells of the rendered selection, or null. */
@@ -730,6 +1019,7 @@ internal class GhosttyTerminalView(context: Context) : View(context) {
 
   private fun wantsBlink(): Boolean {
     return cursorBlinks && cursorVisible && isAttachedToWindow && hasWindowFocus() &&
+      SystemClock.uptimeMillis() - lastTerminalActivityAt < BLINK_IDLE_STOP_MS &&
       Settings.Global.getFloat(
         context.contentResolver, Settings.Global.ANIMATOR_DURATION_SCALE, 1f
       ) != 0f
@@ -754,10 +1044,13 @@ internal class GhosttyTerminalView(context: Context) : View(context) {
   private fun holdBlinkSolid() {
     if (!blinkPhaseOn) {
       blinkPhaseOn = true
       invalidate()
     }
-    if (blinkScheduled) {
-      removeCallbacks(blinkRunnable)
-      postDelayed(blinkRunnable, BLINK_INTERVAL_MS)
+    removeCallbacks(blinkRunnable)
+    if (wantsBlink()) {
+      blinkScheduled = true
+      postDelayed(blinkRunnable, BLINK_INTERVAL_MS)
+    } else {
+      blinkScheduled = false
     }
   }
@@ -777,11 +1070,13 @@ internal class GhosttyTerminalView(context: Context) : View(context) {
     syncBlinkTimer()
   }
 
-  private fun drawRowRecord(buf: ByteBuffer, canvas: Canvas, snapCols: Int) {
+  private fun drawRowRecord(buf: ByteBuffer, canvas: Canvas, snapCols: Int, clearRow: Boolean = true) {
     val rowIndex = buf.int
     val selStart = buf.int
     val selEnd = buf.int
     val textUnits = buf.int
+    val wrapContinuation = buf.int == 1
+    if (rowIndex < rowWrapCont.size) rowWrapCont[rowIndex] = wrapContinuation
     if (rowIndex < rowSelStart.size) {
       rowSelStart[rowIndex] = selStart
       rowSelEnd[rowIndex] = selEnd
@@ -836,12 +1131,18 @@ internal class GhosttyTerminalView(context: Context) : View(context) {
     }
     buf.position(nextRecord)
 
+    updateRowLinks(rowIndex, textUnits, wrapContinuation)
+
     val top = (rowIndex * cellHeight).toFloat()
     val bottom = top + cellHeight
 
-    // Background pass: row clear, then per-cell backgrounds.
-    fillPaint.color = defaultBg
-    canvas.drawRect(0f, top, canvas.width.toFloat(), bottom, fillPaint)
+    // Background pass: row clear, then per-cell backgrounds. A row's own
+    // display list starts empty over the view's background, so only the
+    // bitmap, which keeps the previous pixels, needs the clear.
+    if (clearRow) {
+      fillPaint.color = defaultBg
+      canvas.drawRect(0f, top, canvas.width.toFloat(), bottom, fillPaint)
+    }
     for (col in 0 until colCount) {
       val bg = cellBg[col]
       if (bg == defaultBg) continue
@@ -851,12 +1152,20 @@ internal class GhosttyTerminalView(context: Context) : View(context) {
       canvas.drawRect(x, top, x + width, bottom, fillPaint)
     }
 
-    // Text pass.
-    for (col in 0 until colCount) {
+    // Text pass. Neighbouring printable ASCII cells in one style are one
+    // draw: in a face whose every glyph advances exactly one cell they land
+    // where per-cell draws would, and a row costs a few draws instead of one
+    // per column. Everything else is still drawn cell by cell.
+    var col = 0
+    while (col < colCount) {
       val flags = cellFlags[col]
       val len = cellTextLen[col]
-      if (len == 0 || flags and FLAG_SPACER != 0 || flags and FLAG_INVISIBLE != 0) continue
-      val codepoint = Character.codePointAt(rowChars, cellTextOff[col])
+      if (len == 0 || flags and FLAG_SPACER != 0 || flags and FLAG_INVISIBLE != 0) {
+        col++
+        continue
+      }
+      val offset = cellTextOff[col]
+      val codepoint = Character.codePointAt(rowChars, offset)
       val paint = when {
         symbolsPaint != null &&
           (codepoint in PUA_BMP_FIRST..PUA_BMP_LAST || codepoint >= PUA_SUPPLEMENTARY_FIRST) ->
@@ -868,24 +1177,123 @@ internal class GhosttyTerminalView(context: Context) : View(context) {
       }
       paint.color = cellFg[col]
       if (flags and FLAG_FAINT != 0) paint.alpha = 160
-      val x = col * cellWidth
-      canvas.drawText(rowChars, cellTextOff[col], len, x, top + baseline, paint)
-      val runWidth = if (flags and FLAG_WIDE != 0) cellWidth * 2 else cellWidth
-      if (flags and FLAG_UNDERLINE != 0) {
-        canvas.drawRect(x, bottom - underlineThickness, x + runWidth, bottom, paint)
+      var end = col + 1
+      var inked = len != 1 || codepoint != SPACE
+      if (asciiCell(col) && advancesOneCell(paint)) {
+        while (end < colCount && asciiCell(end) && cellTextOff[end] == offset + (end - col) &&
+          cellFg[end] == cellFg[col] && cellFlags[end] and RUN_STYLE == flags and RUN_STYLE
+        ) {
+          if (rowChars[cellTextOff[end]].code != SPACE) inked = true
+          end++
+        }
       }
-      if (flags and FLAG_STRIKETHROUGH != 0) {
-        val mid = top + cellHeight / 2f
-        canvas.drawRect(x, mid - underlineThickness / 2, x + runWidth, mid + underlineThickness / 2, paint)
+      val x = col * cellWidth
+      if (inked) canvas.drawText(rowChars, offset, if (end > col + 1) end - col else len, x, top + baseline, paint)
+      for (cell in col until end) {
+        val cellFlagsNow = cellFlags[cell]
+        val cellX = cell * cellWidth
+        val runWidth = if (cellFlagsNow and FLAG_WIDE != 0) cellWidth * 2 else cellWidth
+        if (cellFlagsNow and FLAG_UNDERLINE != 0 || cellFlagsNow and FLAG_HYPERLINK != 0 ||
+          rowLinkMask.getOrNull(rowIndex)?.getOrNull(cell) == true
+        ) {
+          canvas.drawRect(cellX, bottom - underlineThickness, cellX + runWidth, bottom, paint)
+        }
+        if (cellFlagsNow and FLAG_STRIKETHROUGH != 0) {
+          val mid = top + cellHeight / 2f
+          canvas.drawRect(cellX, mid - underlineThickness / 2, cellX + runWidth, mid + underlineThickness / 2, paint)
+        }
       }
       paint.alpha = 255
+      col = end
     }
   }
 
+  /** One UTF-16 unit of printable ASCII in a single-width, drawable cell. */
+  private fun asciiCell(col: Int): Boolean {
+    if (cellTextLen[col] != 1) return false
+    if (cellFlags[col] and (FLAG_WIDE or FLAG_SPACER or FLAG_INVISIBLE) != 0) return false
+    return rowChars[cellTextOff[col]].code in SPACE..0x7E
+  }
+
+  // Per paint, whether an ASCII glyph advances exactly one cell at the
+  // current size; a run is only drawn when it does.
+  private val cellAdvanceChecked = HashMap<Paint, Boolean>()
+  private var cellAdvanceWidth = -1f
+
+  private fun advancesOneCell(paint: Paint): Boolean {
+    if (cellAdvanceWidth != cellWidth) {
+      cellAdvanceChecked.clear()
+      cellAdvanceWidth = cellWidth
+    }
+    return cellAdvanceChecked.getOrPut(paint) {
+      kotlin.math.abs(paint.measureText("M") - cellWidth) < 0.001f &&
+        kotlin.math.abs(paint.measureText("i") - cellWidth) < 0.001f
+    }
+  }
+
+  private interface RowRenderer {
+    fun resize(rows: Int)
+    fun clear()
+    fun release()
+    fun recordRow(buf: ByteBuffer, snapCols: Int)
+    fun draw(canvas: Canvas, rows: Int)
+  }
+
+  @RequiresApi(Build.VERSION_CODES.Q)
+  private class RowDisplayLists(private val view: GhosttyTerminalView) : RowRenderer {
+    private var nodes = emptyArray<RenderNode>()
+    // Consumes a record for a row the grid no longer has.
+    private var scratchRow: RenderNode? = null
+
+    override fun resize(rows: Int) {
+      val previous = nodes
+      nodes = Array(rows) { previous.getOrNull(it) ?: RenderNode("ghostty-row").apply { setClipToBounds(false) } }
+      for (row in rows until previous.size) previous[row].discardDisplayList()
+    }
+
+    override fun clear() { nodes.forEach { it.discardDisplayList() } }
+
+    override fun release() {
+      clear()
+      scratchRow?.discardDisplayList()
+      scratchRow = null
+      nodes = emptyArray()
+    }
+
+    override fun recordRow(buf: ByteBuffer, snapCols: Int) {
+      val rowIndex = buf.getInt(buf.position())
+      val node = nodes.getOrNull(rowIndex) ?: (scratchRow ?: RenderNode("ghostty-scratch").also { scratchRow = it })
+      val widthPx = max(1, ceil(view.cols * view.cellWidth).toInt())
+      val top = rowIndex * view.cellHeight
+      node.setPosition(0, top, widthPx, top + view.cellHeight)
+      val recording = node.beginRecording(widthPx, view.cellHeight)
+      try {
+        // drawRowRecord paints in grid coordinates.
+        recording.translate(0f, -top.toFloat())
+        view.drawRowRecord(buf, recording, snapCols, clearRow = false)
+      } finally {
+        node.endRecording()
+      }
+    }
+
+    override fun draw(canvas: Canvas, rows: Int) {
+      for (row in 0 until minOf(rows, nodes.size)) {
+        val node = nodes[row]
+        if (node.hasDisplayList()) canvas.drawRenderNode(node)
+      }
+    }
+  }
+
   override fun onDraw(canvas: Canvas) {
     canvas.drawColor(defaultBg)
-    bitmap?.let { canvas.drawBitmap(it, 0f, 0f, null) }
+    // Row display lists replay only on a hardware canvas; older releases
+    // use the bitmap fallback.
+    if (rowDisplayLists != null && canvas.isHardwareAccelerated) {
+      rowDisplayLists.draw(canvas, rows)
+    } else {
+      bitmap?.let { canvas.drawBitmap(it, 0f, 0f, null) }
+    }
     drawCursor(canvas)
     drawSelectionHandles(canvas)
     drawScrollIndicator(canvas)
     drawJumpToBottomChip(canvas)
@@ -940,34 +1348,48 @@ internal class GhosttyTerminalView(context: Context) : View(context) {
     canvas.drawPath(jumpChipArrow, jumpChipPaint)
   }
 
   private fun drawCursor(canvas: Canvas) {
     if (!cursorVisible || cursorX < 0 || cursorY < 0) return
     if (cursorBlinks && blinkScheduled && !blinkPhaseOn) return
     val x = cursorX * cellWidth
     val top = (cursorY * cellHeight).toFloat()
     val bottom = top + cellHeight
     cursorPaint.color = if (cursorColor != 0) cursorColor else defaultFg
     when (cursorStyle) {
       CURSOR_STYLE_BAR -> {
         cursorPaint.style = Paint.Style.FILL
         canvas.drawRect(x, top, x + underlineThickness * 2, bottom, cursorPaint)
       }
       CURSOR_STYLE_UNDERLINE -> {
         cursorPaint.style = Paint.Style.FILL
         canvas.drawRect(x, bottom - underlineThickness * 2, x + cellWidth, bottom, cursorPaint)
       }
       CURSOR_STYLE_BLOCK_HOLLOW -> {
         cursorPaint.style = Paint.Style.STROKE
         cursorPaint.strokeWidth = underlineThickness
         canvas.drawRect(x, top, x + cellWidth, bottom, cursorPaint)
       }
       else -> {
         cursorPaint.style = Paint.Style.FILL
         cursorPaint.alpha = 170
         canvas.drawRect(x, top, x + cellWidth, bottom, cursorPaint)
         cursorPaint.alpha = 255
       }
     }
   }
-
+
+  /** Explicit keyboard control; pointer focus alone must not open the IME. */
+  fun showKeyboard() {
+    if (handle == 0L || finished || !isAttachedToWindow) return
+    requestFocus()
+    context.getSystemService(InputMethodManager::class.java)?.showSoftInput(this, 0)
+  }
+
+  /** Symmetric explicit control; hides the IME with this terminal's window token and drops focus. */
+  fun hideKeyboard() {
+    if (!isAttachedToWindow || windowToken == null) return
+    context.getSystemService(InputMethodManager::class.java)?.hideSoftInputFromWindow(windowToken, 0)
+    if (hasFocus()) clearFocus()
+  }
+
   // ── Input ──
@@ -1094,17 +1516,26 @@ internal class GhosttyTerminalView(context: Context) : View(context) {
     }
   }
 
-  private companion object {
+  internal companion object {
     const val FONT_SIZE_DP = 14f
     // Same bounds and step as the iOS pinch-zoom (UITerminalView).
     const val MIN_FONT_SIZE_DP = 4f
     const val MAX_FONT_SIZE_DP = 64f
     const val PINCH_SCALE_STEP = 0.1f
+    // A reported pinch is quiet between changes smaller than this.
+    const val PINCH_REPORT_STEP = 0.02f
     const val INITIAL_COLS = 80
     const val INITIAL_ROWS = 24
     const val MAX_SCROLLBACK = 10_000L
     const val BLINK_INTERVAL_MS = 600L
+    /** A quiet terminal holds a solid cursor: 0 idle frames, 0 idle GPU. */
+    const val BLINK_IDLE_STOP_MS = 30_000L
     const val SYMBOLS_FONT_ASSET = "SymbolsNerdFontMono-Regular.ttf"
+    // Bundled by the app (android/app/src/main/assets); checked by
+    // scripts/diagnostics/application/checkTerminalFont.mjs.
+    const val TEXT_FONT_ASSET = "JetBrainsMono-Regular.ttf"
+    const val TEXT_FONT_ITALIC_ASSET = "JetBrainsMono-Italic.ttf"
+    const val TEXT_FONT_BOLD_ASSET = "JetBrainsMono-SemiBold.ttf"
 
     // Nerd Font glyph ranges: BMP private-use area plus the supplementary
     // private-use planes (material icons live at U+F0000+ since v3).
@@ -1131,7 +1562,7 @@ internal class GhosttyTerminalView(context: Context) : View(context) {
     const val JUMP_CHIP_ARROW_COLOR = 0xFFE0E0E0.toInt()
 
     const val HEADER_BYTES = 52
-    const val ROW_HEADER_BYTES = 16
+    const val ROW_HEADER_BYTES = 20
     const val CELL_RECORD_BYTES = 16
     const val MAX_CELL_TEXT_UNITS = 32
 
@@ -1153,5 +1584,14 @@ internal class GhosttyTerminalView(context: Context) : View(context) {
     const val FLAG_SPACER = 1 shl 8
     const val FLAG_FG_DEFAULT = 1 shl 9
     const val FLAG_BG_NONE = 1 shl 10
+    const val FLAG_HYPERLINK = 1 shl 11
+    /** The flags that choose a run's paint and alpha; the rest draw per cell. */
+    private const val RUN_STYLE = FLAG_BOLD or FLAG_ITALIC or FLAG_FAINT
+    private const val SPACE = 0x20
+
+    /** Plain URLs join at most this many characters across wrapped rows. */
+    const val LINK_MAX_CHARS = 4096
+    /** URL delimiters trimmed off a joined link's tail, xterm-style. */
+    const val URL_TRAILING_TRIM = ".,:!?\"')]}>|~[]"
   }
 }
diff --git a/node_modules/expo-libghostty/android/src/main/java/expo/modules/libghostty/GhosttyVt.kt b/node_modules/expo-libghostty/android/src/main/java/expo/modules/libghostty/GhosttyVt.kt
index 8cfe86e..5c39915 100644
--- a/node_modules/expo-libghostty/android/src/main/java/expo/modules/libghostty/GhosttyVt.kt
+++ b/node_modules/expo-libghostty/android/src/main/java/expo/modules/libghostty/GhosttyVt.kt
@@ -75,6 +75,9 @@ internal object GhosttyVt {
   /** Select the word at a viewport cell; false when there is nothing there. */
   external fun nativeSelectWord(handle: Long, col: Int, row: Int): Boolean
 
+  /** OSC 8 hyperlink URI at a viewport cell, or null when the cell has none. */
+  external fun nativeHyperlinkUri(handle: Long, col: Int, row: Int): String?
+
   external fun nativeSelectAll(handle: Long): Boolean
 
   /** Install a linear selection between two viewport cells (inclusive). */
diff --git a/node_modules/expo-libghostty/build/ExpoLibghostty.types.d.ts b/node_modules/expo-libghostty/build/ExpoLibghostty.types.d.ts
index 0969f13..f21ebb7 100644
--- a/node_modules/expo-libghostty/build/ExpoLibghostty.types.d.ts
+++ b/node_modules/expo-libghostty/build/ExpoLibghostty.types.d.ts
@@ -18,6 +18,10 @@ export type TerminalViewRef = {
     writeText(text: string): Promise<void>;
     /** Mark the underlying PTY as exited. */
     finish(exitCode: number): Promise<void>;
+    /** Explicitly open the software keyboard. */
+    showKeyboard(): Promise<void>;
+    /** Explicitly hide the software keyboard and release focus. */
+    hideKeyboard(): Promise<void>;
 };
 export type TerminalTheme = {
     /**
@@ -52,6 +56,8 @@ export type TerminalViewProps = {
      * pass undefined fields to keep ghostty's defaults.
      */
     theme?: TerminalTheme;
+  /** Allow taps to open the software keyboard. Defaults to true. */
+  autoShowKeyboard?: boolean;
     /** User keyboard/IME input to forward to the PTY. */
     onInput?: (event: {
         nativeEvent: TerminalInputEvent;
@@ -60,6 +66,26 @@ export type TerminalViewProps = {
     onResize?: (event: {
         nativeEvent: TerminalResizeEvent;
     }) => void;
+    onScroll?: (event: {
+        nativeEvent: {
+            rows: number;
+        };
+    }) => void;
+    /** The user activated a link printed in the grid (plain URL or OSC 8).
+     *  The URL is exact terminal output; validate schemes before opening. */
+    onOpenLink?: (event: {
+        nativeEvent: {
+            url: string;
+        };
+    }) => void;
+    /** A pinch, reported instead of zooming when set: the host owns text size
+     *  and hands it back through `fontSize`. `scale` is since the pinch began. */
+    onPinch?: (event: {
+        nativeEvent: {
+            phase: 'begin' | 'change' | 'end';
+            scale: number;
+        };
+    }) => void;
     ref?: Ref<TerminalViewRef>;
     style?: StyleProp<ViewStyle>;
 };
diff --git a/node_modules/expo-libghostty/build/ExpoLibghosttyView.js b/node_modules/expo-libghostty/build/ExpoLibghosttyView.js
index e3bf3b8..dc5b367 100644
--- a/node_modules/expo-libghostty/build/ExpoLibghosttyView.js
+++ b/node_modules/expo-libghostty/build/ExpoLibghosttyView.js
@@ -19,6 +19,8 @@ export default function TerminalView({ ref, onResize, ...props }) {
             });
         };
         return {
+            showKeyboard: whenReady(() => native.current.showKeyboard()),
+            hideKeyboard: whenReady(() => native.current.hideKeyboard()),
             write: whenReady((base64) => native.current.write(base64)),
             writeText: whenReady((text) => native.current.writeText(text)),
             finish: whenReady((exitCode) => native.current.finish(exitCode)),
diff --git a/node_modules/expo-libghostty/ios/ExpoLibghosttyModule.swift b/node_modules/expo-libghostty/ios/ExpoLibghosttyModule.swift
index 2f32970..5acecaa 100644
--- a/node_modules/expo-libghostty/ios/ExpoLibghosttyModule.swift
+++ b/node_modules/expo-libghostty/ios/ExpoLibghosttyModule.swift
@@ -5,7 +5,7 @@ public class ExpoLibghosttyModule: Module {
     Name("ExpoLibghostty")
 
     View(ExpoLibghosttyView.self) {
-      Events("onInput", "onResize")
+      Events("onInput", "onResize", "onScroll", "onOpenLink", "onPinch")
 
       // Base font size in points (default 14). Changing it after mount
       // rebuilds the surface and resets the grid.
@@ -13,6 +13,10 @@ public class ExpoLibghosttyModule: Module {
         view.fontSize = size
       }
 
+      Prop("autoShowKeyboard") { (view: ExpoLibghosttyView, enabled: Bool) in
+        view.autoShowKeyboard = enabled
+      }
+
       // Theme colors (ghostty config syntax); nil clears to the defaults.
       // App-wide on iOS: applied through the shared controller's config.
       Prop("theme") { (view: ExpoLibghosttyView, theme: TerminalThemeRecord?) in
@@ -32,6 +36,14 @@ public class ExpoLibghosttyModule: Module {
         view.write(text)
       }.runOnQueue(.main)
 
+      AsyncFunction("showKeyboard") { (view: ExpoLibghosttyView) in
+        view.showKeyboard()
+      }.runOnQueue(.main)
+
+      AsyncFunction("hideKeyboard") { (view: ExpoLibghosttyView) in
+        view.hideKeyboard()
+      }.runOnQueue(.main)
+
       // PTY exited.
       AsyncFunction("finish") { (view: ExpoLibghosttyView, exitCode: UInt32) in
         view.finish(exitCode: exitCode)
diff --git a/node_modules/expo-libghostty/ios/ExpoLibghosttyView.swift b/node_modules/expo-libghostty/ios/ExpoLibghosttyView.swift
index 12925b5..91cefe9 100644
--- a/node_modules/expo-libghostty/ios/ExpoLibghosttyView.swift
+++ b/node_modules/expo-libghostty/ios/ExpoLibghosttyView.swift
@@ -1,5 +1,6 @@
 import ExpoModulesCore
 import GhosttyTerminal
+import UIKit
 
 // Ghostty-backed terminal view. The JS side owns transport and session
 // lifecycle; this view only renders bytes pushed via `write` and reports
@@ -44,6 +45,14 @@ class ExpoLibghosttyView: ExpoView {
 
   let onInput = EventDispatcher()
   let onResize = EventDispatcher()
+  let onScroll = EventDispatcher()
+  let onOpenLink = EventDispatcher()
+  let onPinch = EventDispatcher()
+  var autoShowKeyboard = true {
+    didSet { terminalView.autoShowKeyboard = autoShowKeyboard }
+  }
+  private var cellHeightPoints: CGFloat = 18
+  private var scrollRemainder: CGFloat = 0
 
   required init(appContext: AppContext? = nil) {
     super.init(appContext: appContext)
@@ -57,7 +66,14 @@ class ExpoLibghosttyView: ExpoView {
         ])
       },
       resize: { [weak self] viewport in
-        self?.onResize(["cols": viewport.columns, "rows": viewport.rows])
+        guard let self else { return }
+        if viewport.cellHeightPixels > 0 {
+          self.cellHeightPoints = CGFloat(viewport.cellHeightPixels) / UIScreen.main.scale
+        }
+        self.onResize([
+          "cols": viewport.columns,
+          "rows": viewport.rows,
+        ])
       }
     )
     self.session = session
@@ -65,8 +81,22 @@ class ExpoLibghosttyView: ExpoView {
     // ("surface rebuild skipped: missing controller").
     terminalView.controller = TerminalController.shared
     terminalView.configuration = TerminalSurfaceOptions(backend: .inMemory(session))
+    terminalView.inputAccessoryItems = []
     terminalView.autoresizingMask = [.flexibleWidth, .flexibleHeight]
+    terminalView.delegate = self
     addSubview(terminalView)
+    terminalView.hostScrollHandler = { [weak self] deltaY in
+      guard let self else { return }
+      self.scrollRemainder -= deltaY
+      let rows = Int(self.scrollRemainder / self.cellHeightPoints)
+      if rows != 0 {
+        self.scrollRemainder -= CGFloat(rows) * self.cellHeightPoints
+        self.onScroll(["rows": rows])
+      }
+    }
+    terminalView.hostPinchHandler = { [weak self] phase, scale in
+      self?.onPinch(["phase": phase, "scale": Double(scale)])
+    }
   }
 
   /// Feed PTY output (terminal.output on the wire) into the grid.
@@ -79,8 +109,33 @@ class ExpoLibghosttyView: ExpoView {
     session?.receive(text)
   }
 
+  /// Raise the software keyboard. The accessory bar stays empty either way.
+  func showKeyboard() {
+    terminalView.showKeyboard()
+  }
+
+  /// Drop the software keyboard, as a tap on the grid already does.
+  func hideKeyboard() {
+    terminalView.hideKeyboard()
+  }
+
   /// Mark the underlying PTY as exited (terminal.exit on the wire).
   func finish(exitCode: UInt32) {
     session?.finish(exitCode: exitCode, runtimeMilliseconds: 0)
   }
 }
+
+extension ExpoLibghosttyView: TerminalSurfaceOpenURLDelegate, TerminalSurfaceCopyURLDelegate {
+  /// Core recognized the user activating a printed link (plain URL or OSC 8).
+  /// The JS layer owns the scheme boundary; nothing is opened natively.
+  func terminalDidRequestOpenURL(_ url: String, kind: TerminalOpenURLKind) {
+    onOpenLink(["url": url])
+  }
+
+  func terminalDidRequestCopyURL(_ url: String) {
+    guard !url.isEmpty else { return }
+    UIPasteboard.general.string = url
+    UIImpactFeedbackGenerator(style: .medium).impactOccurred()
+    UIAccessibility.post(notification: .announcement, argument: "Link copied")
+  }
+}
diff --git a/node_modules/expo-libghostty/ios/vendor/GhosttyTerminal/InMemory/TerminalCallbackBridge.swift b/node_modules/expo-libghostty/ios/vendor/GhosttyTerminal/InMemory/TerminalCallbackBridge.swift
index d90375a..85a34b9 100644
--- a/node_modules/expo-libghostty/ios/vendor/GhosttyTerminal/InMemory/TerminalCallbackBridge.swift
+++ b/node_modules/expo-libghostty/ios/vendor/GhosttyTerminal/InMemory/TerminalCallbackBridge.swift
@@ -20,6 +20,9 @@ final class TerminalCallbackBridge {
     nonisolated(unsafe) var rawSurface: ghostty_surface_t?
     var onCellSizeChange: ((UInt32, UInt32) -> Void)?
     var onRenderRequest: (() -> Void)?
+    /// Link presence under the pointer changed. Touch views use this to
+    /// prime a synchronous link lookup (touch has no hover of its own).
+    var onMouseOverLink: ((String?) -> Void)?
 
     init(delegate: (any TerminalSurfaceViewDelegate)? = nil) {
         self.delegate = delegate
@@ -129,6 +132,7 @@ final class TerminalCallbackBridge {
             )
             (delegate as? any TerminalSurfaceHoverLinkDelegate)?
                 .terminalDidUpdateHoverLink(url)
+            onMouseOverLink?(url)
 
         case GHOSTTY_ACTION_PWD:
             let payload = action.action.pwd
diff --git a/node_modules/expo-libghostty/ios/vendor/GhosttyTerminal/Platform/UIKit/UITerminalView+Interaction.swift b/node_modules/expo-libghostty/ios/vendor/GhosttyTerminal/Platform/UIKit/UITerminalView+Interaction.swift
index 3f3eeb2..2e53ab7 100644
--- a/node_modules/expo-libghostty/ios/vendor/GhosttyTerminal/Platform/UIKit/UITerminalView+Interaction.swift
+++ b/node_modules/expo-libghostty/ios/vendor/GhosttyTerminal/Platform/UIKit/UITerminalView+Interaction.swift
@@ -25,9 +25,22 @@
                 touchDidScrollDuringCurrentTouch = false
                 if softwareKeyboardVisible {
                     pendingKeyboardDismissOnTouchEnd = true
+                } else if autoShowKeyboard {
+                    // Deferred to touch end: a tap that activates a link must
+                    // not raise the keyboard, and the link decision needs the
+                    // stable end point.
+                    pendingKeyboardShowOnTouchEnd = true
                 } else {
+                    softwareKeyboardSuppressed = true
                     becomeFirstResponder()
                 }
+                if touches.count == 1, let touch = touches.first, touch.type == .direct {
+                    let point = touch.location(in: self)
+                    pendingTapPoint = point
+                    primeHoverLink(at: point)
+                } else {
+                    pendingTapPoint = nil
+                }
             #endif
         }
 
@@ -49,6 +62,29 @@
                 return
             }
             #if !targetEnvironment(macCatalyst)
+                defer {
+                    pendingKeyboardShowOnTouchEnd = false
+                    pendingTapPoint = nil
+                }
+                if let start = pendingTapPoint,
+                   !touchDidScrollDuringCurrentTouch,
+                   event?.allTouches?.count == 1,
+                   let touch = touches.first {
+                    let point = touch.location(in: self)
+                    if hypot(point.x - start.x, point.y - start.y) <= 10 {
+                        if hoverLink(at: point) != nil {
+                            activateLink(at: point)
+                            // A link activation must not raise or dismiss the
+                            // keyboard; the browser opens over what is there.
+                            pendingKeyboardDismissOnTouchEnd = false
+                            pendingKeyboardShowOnTouchEnd = false
+                        } else if pendingKeyboardShowOnTouchEnd {
+                            showKeyboard()
+                        }
+                    }
+                } else if pendingKeyboardShowOnTouchEnd, !touchDidScrollDuringCurrentTouch {
+                    showKeyboard()
+                }
                 if pendingKeyboardDismissOnTouchEnd, !touchDidScrollDuringCurrentTouch {
                     resignFirstResponder()
                 }
@@ -67,6 +103,8 @@
             }
             #if !targetEnvironment(macCatalyst)
                 pendingKeyboardDismissOnTouchEnd = false
+                pendingKeyboardShowOnTouchEnd = false
+                pendingTapPoint = nil
                 touchDidScrollDuringCurrentTouch = false
             #endif
             super.touchesCancelled(touches, with: event)
@@ -448,10 +486,54 @@
                 }
             }
 
+            /// Prime core's link hover state at the touch-down point; the
+            /// MOUSE_OVER_LINK action reports the result asynchronously.
+            func primeHoverLink(at point: CGPoint) {
+                guard let surface else { return }
+                pendingHoverPoint = point
+                surface.sendMousePos(
+                    x: Double(point.x),
+                    y: Double(point.y),
+                    mods: ghostty_input_mods_e(rawValue: 0)
+                )
+            }
+
+            /// The link core reported under `point`, or nil. The stored point
+            /// must be within tap drift, so entries from other locations or
+            /// reports that never arrived cannot produce a false hit.
+            func hoverLink(at point: CGPoint) -> String? {
+                guard let hover = lastHoverLink,
+                      hypot(hover.point.x - point.x, hover.point.y - point.y) <= 10
+                else { return nil }
+                return hover.url
+            }
+
+            /// Activate the link under `point` through core's own open-URL
+            /// recognition. The super modifier is the bypass gesture: core
+            /// opens the link itself and never forwards the click to a
+            /// mouse-reporting program, and a click on plain grid text is a
+            /// no-op rather than a selection.
+            func activateLink(at point: CGPoint) {
+                guard let surface else { return }
+                let mods = ghostty_input_mods_e(rawValue: GHOSTTY_MODS_SUPER.rawValue)
+                surface.sendMousePos(x: Double(point.x), y: Double(point.y), mods: mods)
+                _ = surface.sendMouseButton(state: GHOSTTY_MOUSE_PRESS, button: GHOSTTY_MOUSE_LEFT, mods: mods)
+                _ = surface.sendMouseButton(state: GHOSTTY_MOUSE_RELEASE, button: GHOSTTY_MOUSE_LEFT, mods: mods)
+            }
+
             @objc func handleLongPressForSelection(
                 _ gesture: UILongPressGestureRecognizer
             ) {
                 guard gesture.state == .began else { return }
+                if let url = hoverLink(at: gesture.location(in: self)) {
+                    stopMomentumScrolling()
+                    // The lift must not re-read this gesture as a tap: copy
+                    // and open are mutually exclusive.
+                    pendingTapPoint = nil
+                    pendingKeyboardShowOnTouchEnd = false
+                    (delegate as? any TerminalSurfaceCopyURLDelegate)?.terminalDidRequestCopyURL(url)
+                    return
+                }
                 guard let delegate = delegate as? any TerminalSurfaceTextSelectionRequestDelegate else { return }
                 guard let surface else { return }
                 guard case let .inMemory(session) = configuration.backend else {
@@ -532,12 +614,16 @@
                     "touch scroll changed translation=\(String(format: "%.2f", translation.x))x\(String(format: "%.2f", translation.y))"
                 )
 
-                let scrollMods = TerminalScrollModifiers(precision: true)
-                surface?.sendMouseScroll(
-                    x: Double(translation.x * touchScrollMultiplier),
-                    y: Double(translation.y * touchScrollMultiplier),
-                    mods: scrollMods.rawValue
-                )
+                if let hostScrollHandler {
+                    hostScrollHandler(translation.y)
+                } else {
+                    let scrollMods = TerminalScrollModifiers(precision: true)
+                    surface?.sendMouseScroll(
+                        x: Double(translation.x * touchScrollMultiplier),
+                        y: Double(translation.y * touchScrollMultiplier),
+                        mods: scrollMods.rawValue
+                    )
+                }
 
             case .ended:
                 guard activePointerButton == nil else { return }
@@ -566,8 +652,10 @@
                 "momentum start velocity=\(String(format: "%.2f", velocity.x))x\(String(format: "%.2f", velocity.y))"
             )
 
-            let mods = TerminalScrollModifiers(precision: true, momentum: .began)
-            surface?.sendMouseScroll(x: 0, y: 0, mods: mods.rawValue)
+            if hostScrollHandler == nil {
+                let mods = TerminalScrollModifiers(precision: true, momentum: .began)
+                surface?.sendMouseScroll(x: 0, y: 0, mods: mods.rawValue)
+            }
 
             let link = CADisplayLink(
                 target: self,
@@ -597,19 +685,23 @@
                 "momentum frame velocity=\(String(format: "%.2f", momentumVelocity.x))x\(String(format: "%.2f", momentumVelocity.y)) delta=\(String(format: "%.2f", deltaX))x\(String(format: "%.2f", deltaY))"
             )
 
-            let mods = TerminalScrollModifiers(precision: true, momentum: .changed)
-            surface?.sendMouseScroll(
-                x: Double(deltaX),
-                y: Double(deltaY),
-                mods: mods.rawValue
-            )
+            if let hostScrollHandler {
+                hostScrollHandler(momentumVelocity.y * dt)
+            } else {
+                let mods = TerminalScrollModifiers(precision: true, momentum: .changed)
+                surface?.sendMouseScroll(
+                    x: Double(deltaX),
+                    y: Double(deltaY),
+                    mods: mods.rawValue
+                )
+            }
         }
 
         func stopMomentumScrolling(sendTerminalEndEvent: Bool = true) {
             guard momentumDisplayLink != nil else { return }
             TerminalDebugLog.log(.input, "momentum stop")
 
-            if sendTerminalEndEvent {
+            if sendTerminalEndEvent && hostScrollHandler == nil {
                 let mods = TerminalScrollModifiers(precision: true, momentum: .none)
                 surface?.sendMouseScroll(x: 0, y: 0, mods: mods.rawValue)
             }
@@ -630,6 +722,7 @@
         ) -> Bool {
             if gestureRecognizer is UILongPressGestureRecognizer {
                 return (delegate as? any TerminalSurfaceTextSelectionRequestDelegate) != nil
+                    || (delegate as? any TerminalSurfaceCopyURLDelegate) != nil
             }
             return true
         }
diff --git a/node_modules/expo-libghostty/ios/vendor/GhosttyTerminal/Platform/UIKit/UITerminalView+PinchZoom.swift b/node_modules/expo-libghostty/ios/vendor/GhosttyTerminal/Platform/UIKit/UITerminalView+PinchZoom.swift
index 10834a3..80d4847 100644
--- a/node_modules/expo-libghostty/ios/vendor/GhosttyTerminal/Platform/UIKit/UITerminalView+PinchZoom.swift
+++ b/node_modules/expo-libghostty/ios/vendor/GhosttyTerminal/Platform/UIKit/UITerminalView+PinchZoom.swift
@@ -8,6 +8,8 @@
 
     extension UITerminalView {
         private static let scaleStepThreshold: CGFloat = 0.1
+        /// A reported pinch is quiet between changes smaller than this.
+        private static let scaleReportThreshold: CGFloat = 0.02
 
         func setupPinchZoomGesture() {
             let pinch = UIPinchGestureRecognizer(
@@ -21,12 +23,23 @@
             switch gesture.state {
             case .began:
                 lastPinchScale = gesture.scale
+                if let hostPinchHandler {
+                    hostPinchHandler("begin", gesture.scale)
+                    return
+                }
                 TerminalDebugLog.log(
                     .actions,
                     "pinch began scale=\(String(format: "%.3f", gesture.scale)) fontSize=\(currentFontSize)"
                 )
 
             case .changed:
+                if let hostPinchHandler {
+                    if abs(gesture.scale - lastPinchScale) >= Self.scaleReportThreshold {
+                        lastPinchScale = gesture.scale
+                        hostPinchHandler("change", gesture.scale)
+                    }
+                    return
+                }
                 let delta = gesture.scale - lastPinchScale
 
                 let steps = Int(delta / Self.scaleStepThreshold)
@@ -66,6 +79,7 @@
 
             case .ended, .cancelled, .failed:
                 lastPinchScale = 1.0
+                hostPinchHandler?("end", gesture.scale)
                 TerminalDebugLog.log(
                     .actions,
                     "pinch ended state=\(gesture.state.rawValue) fontSize=\(currentFontSize)"
diff --git a/node_modules/expo-libghostty/ios/vendor/GhosttyTerminal/Platform/UIKit/UITerminalView.swift b/node_modules/expo-libghostty/ios/vendor/GhosttyTerminal/Platform/UIKit/UITerminalView.swift
index 7bb1e98..a06f817 100644
--- a/node_modules/expo-libghostty/ios/vendor/GhosttyTerminal/Platform/UIKit/UITerminalView.swift
+++ b/node_modules/expo-libghostty/ios/vendor/GhosttyTerminal/Platform/UIKit/UITerminalView.swift
@@ -36,13 +36,39 @@
         lazy var inputHandler = TerminalTextInputHandler(view: self)
         weak var _inputDelegate: (any UITextInputDelegate)?
         var onFocusChange: ((Bool) -> Void)?
+        open var hostScrollHandler: ((CGFloat) -> Void)?
+        /// Set when the host owns text size: a pinch is reported ("begin",
+        /// "change" with the scale since it began, "end") instead of zooming.
+        open var hostPinchHandler: ((String, CGFloat) -> Void)?
+        /// Whether a tap raises the software keyboard, matching Android. Focus is
+        /// taken either way, so hardware and accessory keys keep working while the
+        /// software keyboard stays down.
+        open var autoShowKeyboard = true
 
         #if !targetEnvironment(macCatalyst)
             lazy var terminalInputAccessory = TerminalInputAccessoryView(terminalView: self)
             let stickyModifiers = TerminalStickyModifierState()
             var softwareKeyboardVisible = false
             var pendingKeyboardDismissOnTouchEnd = false
+            /// An empty input view is the only way to hold first responder without
+            /// the software keyboard: resigning it would take hardware keys with it.
+            /// Becoming first responder is a no-op when the view already is one, so
+            /// a change of mind is only honoured by reloading the input views.
+            var softwareKeyboardSuppressed = false {
+                didSet {
+                    guard softwareKeyboardSuppressed != oldValue, isFirstResponder else { return }
+                    reloadInputViews()
+                }
+            }
+            private lazy var suppressedInputView = UIView(frame: .zero)
             var touchDidScrollDuringCurrentTouch = false
+            var pendingKeyboardShowOnTouchEnd = false
+            var pendingTapPoint: CGPoint?
+            var pendingHoverPoint: CGPoint?
+            /// Link core last reported under the primed pointer. Touch has no
+            /// hover, so tap and long-press prime it and read it back when the
+            /// gesture resolves; the stored point keeps stale entries inert.
+            var lastHoverLink: (url: String, point: CGPoint)?
         #endif
 
         #if !targetEnvironment(macCatalyst)
@@ -86,6 +112,31 @@
             true
         }
 
+        #if !targetEnvironment(macCatalyst)
+            override open var inputView: UIView? {
+                softwareKeyboardSuppressed ? suppressedInputView : nil
+            }
+        #endif
+
+        /// Explicit control, mirroring Android's showKeyboard/hideKeyboard.
+        open func showKeyboard() {
+            #if !targetEnvironment(macCatalyst)
+                softwareKeyboardSuppressed = false
+            #endif
+            if isFirstResponder {
+                reloadInputViews()
+            } else {
+                becomeFirstResponder()
+            }
+        }
+
+        /// Releases focus with the keyboard, as Android's clearFocus does, so key
+        /// input stops until a tap takes it back. Suppression is the path that
+        /// keeps a terminal typeable with no keyboard on screen.
+        open func hideKeyboard() {
+            resignFirstResponder()
+        }
+
         override public init(frame: CGRect) {
             super.init(frame: frame)
             commonInit()
@@ -128,6 +179,13 @@
             core.onPostRender = { [weak self] in
                 self?.enforceSublayerScale()
             }
+            #if !targetEnvironment(macCatalyst)
+                core.bridge.onMouseOverLink = { [weak self] url in
+                    guard let self, let point = self.pendingHoverPoint else { return }
+                    self.lastHoverLink = url.map { ($0, point) }
+                    self.pendingHoverPoint = nil
+                }
+            #endif
 
             setupApplicationLifecycleObservers()
             syncApplicationActiveState()
diff --git a/node_modules/expo-libghostty/ios/vendor/GhosttyTerminal/Surface/TerminalSurfaceViewDelegate.swift b/node_modules/expo-libghostty/ios/vendor/GhosttyTerminal/Surface/TerminalSurfaceViewDelegate.swift
index 7a7b11d..41910c7 100644
--- a/node_modules/expo-libghostty/ios/vendor/GhosttyTerminal/Surface/TerminalSurfaceViewDelegate.swift
+++ b/node_modules/expo-libghostty/ios/vendor/GhosttyTerminal/Surface/TerminalSurfaceViewDelegate.swift
@@ -111,6 +111,13 @@ public protocol TerminalSurfaceHoverLinkDelegate: TerminalSurfaceViewDelegate {
     func terminalDidUpdateHoverLink(_ url: String?)
 }
 
+/// Host asked to copy a hyperlink instead of opening it. The URL is the
+/// exact string core recognized (plain printed URL or OSC 8 URI).
+@MainActor
+public protocol TerminalSurfaceCopyURLDelegate: TerminalSurfaceViewDelegate {
+    func terminalDidRequestCopyURL(_ url: String)
+}
+
 /// OSC 7 working-directory update.
 @MainActor
 public protocol TerminalSurfacePwdDelegate: TerminalSurfaceViewDelegate {
diff --git a/node_modules/expo-libghostty/src/ExpoLibghostty.types.ts b/node_modules/expo-libghostty/src/ExpoLibghostty.types.ts
index cbd0ab4..0a0ba5c 100644
--- a/node_modules/expo-libghostty/src/ExpoLibghostty.types.ts
+++ b/node_modules/expo-libghostty/src/ExpoLibghostty.types.ts
@@ -21,6 +21,10 @@ export type TerminalViewRef = {
   writeText(text: string): Promise<void>;
   /** Mark the underlying PTY as exited. */
   finish(exitCode: number): Promise<void>;
+  /** Explicitly open the software keyboard. */
+  showKeyboard(): Promise<void>;
+  /** Explicitly hide the software keyboard and release focus. */
+  hideKeyboard(): Promise<void>;
 };
 
 export type TerminalTheme = {
@@ -57,10 +61,20 @@ export type TerminalViewProps = {
    * pass undefined fields to keep ghostty's defaults.
    */
   theme?: TerminalTheme;
+  /** Allow taps to open the software keyboard. Defaults to true. */
+  autoShowKeyboard?: boolean;
   /** User keyboard/IME input to forward to the PTY. */
   onInput?: (event: { nativeEvent: TerminalInputEvent }) => void;
   /** Grid resized (layout, rotation, font change); forward to the PTY. */
   onResize?: (event: { nativeEvent: TerminalResizeEvent }) => void;
+  /** Scroll gesture in rows, when the host owns scrollback. */
+  onScroll?: (event: { nativeEvent: { rows: number } }) => void;
+  /** The user activated a link printed in the grid (plain URL or OSC 8).
+   *  The URL is exact terminal output; validate schemes before opening. */
+  onOpenLink?: (event: { nativeEvent: { url: string } }) => void;
+  /** A pinch, reported instead of zooming when set: the host owns text size
+   *  and hands it back through `fontSize`. `scale` is since the pinch began. */
+  onPinch?: (event: { nativeEvent: { phase: 'begin' | 'change' | 'end'; scale: number } }) => void;
   ref?: Ref<TerminalViewRef>;
   style?: StyleProp<ViewStyle>;
 };
diff --git a/node_modules/expo-libghostty/src/ExpoLibghosttyView.tsx b/node_modules/expo-libghostty/src/ExpoLibghosttyView.tsx
index 80ad4b1..3907cd3 100644
--- a/node_modules/expo-libghostty/src/ExpoLibghosttyView.tsx
+++ b/node_modules/expo-libghostty/src/ExpoLibghosttyView.tsx
@@ -25,6 +25,8 @@ export default function TerminalView({ ref, onResize, ...props }: TerminalViewPr
         });
       };
     return {
+      showKeyboard: whenReady(() => native.current!.showKeyboard()),
+      hideKeyboard: whenReady(() => native.current!.hideKeyboard()),
       write: whenReady((base64: string) => native.current!.write(base64)),
       writeText: whenReady((text: string) => native.current!.writeText(text)),
       finish: whenReady((exitCode: number) => native.current!.finish(exitCode)),
diff --git a/node_modules/expo-libghostty/vendor-manifest.json b/node_modules/expo-libghostty/vendor-manifest.json
index 80bf1fb..7af4b8b 100644
--- a/node_modules/expo-libghostty/vendor-manifest.json
+++ b/node_modules/expo-libghostty/vendor-manifest.json
@@ -3,8 +3,8 @@
     "repo": "https://github.com/Lakr233/libghostty-spm.git",
     "tag": "1.2.11",
     "xcframework": {
-      "url": "https://github.com/Lakr233/libghostty-spm/releases/download/storage.1.2.11/GhosttyKit.xcframework.zip",
-      "sha256": "584773c3aae93fe231e6aa553032f4ac1a9c4f507538b012f72774ca1354fcd1"
+      "url": "https://github.com/umeranjum17/libghostty-spm/releases/download/storage.1.2.11-muxr.1/GhosttyKit.xcframework.zip",
+      "sha256": "d52148c3541036ddaa495cdd8683ac485c894cdf3a96fd84a00d82ac37da1cc9"
     }
   },
   "MSDisplayLink": {
