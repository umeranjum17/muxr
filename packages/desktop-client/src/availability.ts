/**
 * Whether this build can show a desktop surface at all. The browser answers with
 * a constant so its renderer and input bridge stay out of the first paint.
 */
export { desktopAvailable } from './native';
